# Runtime

This directory owns the normalized Agent runtime reducer, persistence, stream lifecycle, refresh recovery, and keyed concurrent-operation state. User command transaction ordering belongs to `commands`.
