[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    unit: fast unit tests
    integration: slower integration tests that may touch DB/checkpointer
addopts = -v --strict-markers --tb=short -m "not integration"
