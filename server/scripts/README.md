# server/scripts

Temporary debug / ops scripts relocated from `server/` root during architecture-review Phase 1 hygiene.

These are **not** part of the pytest suite (`server/tests/`). Do not import them from production code.
