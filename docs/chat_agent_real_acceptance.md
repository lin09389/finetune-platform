# Chat Agent 真实云端验收清单

本清单用于第十八阶段真实模型验收。每次在 `/chat` 运行任务后，按同一结构记录结果，方便把问题归因到意图路由、工具循环、动作策略、执行恢复或前端展示。

## 固定验收任务

1. 新增一个 `tmp` smoke 文件并运行 typecheck。
2. 给当前项目做一个很小的前端文案调整并运行 typecheck。
3. 修改一个小 CSS 样式并运行 typecheck。
4. 只分析当前项目结构，不写文件。
5. 故意触发一次验证失败，确认最多 repair 一次并清楚停住。

## 单次记录模板

- 时间：
- 使用模型：
- 自主模式：`safe_auto / confirm_all / read_only`
- 用户输入：
- Run ID：
- Workflow ID：
- 是否进入 Agent：
- 是否先读取项目上下文：
- 是否生成 patch：
- patch 结果：自动执行 / 等待审批 / 策略阻断 / 失败
- 是否运行验证命令：
- 验证结果：通过 / 失败 / 未运行
- 失败后是否读取失败摘要：
- 是否触发 repair：
- 最终验收报告是否可见：
- 刷新后状态是否恢复：
- `/workflows` 观测是否一致：
- 问题分类：
  - 未进入 Agent
  - 未读取上下文
  - patch 后未验证
  - 验证失败后未 repair
  - 无最终报告
  - 状态恢复不一致
  - 策略误拦截
  - 策略误放行
- 备注：

## 通过标准

- 每个任务都能在聊天卡片顶部看到最终结果或阻断原因。
- `GET /chat-agent/runs/{run_id}` 能恢复 latest event、latest tool、latest action、验收报告和阻断原因。
- 刷新运行状态不会重复执行已自动执行的 action。
- 失败任务不会静默结束，必须给出失败摘要或人工处理建议。
