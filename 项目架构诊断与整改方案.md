# Finetune Platform 2.0 项目架构诊断与整改方案

## 背景与结论摘要

本文档用于整合现有 `架构.txt` 与 `优化.txt` 两份草稿，形成面向团队内部决策的正式版本。后续关于项目架构收口、优先级排序与整改执行，以本文档为唯一持续维护版本。

当前项目已经明显超出“训练平台 2.0”的初始边界。结合现有代码实现，它正在演进为一个本地 AI 工作台 / AI 平台壳层，能力范围已经覆盖：

- 模型管理
- 数据集管理
- 训练调度与状态管理
- 推理 / 流式聊天
- RAG / Knowledge Base
- Memory / Context 注入
- Agent / Workspace / Task
- Gateway / Heartbeat
- CUA / MCP / 文件与项目操作

核心判断如下：

1. 项目的问题不是“没有功能”，而是“功能扩张过快，但真实打通的核心闭环数量还不够”。
2. `Training` 与 `Chat / Inference / Context` 是当前成熟度最高、最接近真实系统能力的两条主链路。
3. 当前最主要的风险不是继续缺模块，而是边界失控、事实源分裂、伪实现存在、默认配置漂移以及 UI 能力超前于后端真实能力。
4. 项目下一阶段最需要的不是继续加功能，而是做一次架构收口：明确唯一真实入口、清理伪实现、把平台能力收敛为真实可用闭环。

为避免后续资源继续被稀释，项目在产品层面先定义为三条主线：

- 核心主线 1：Training
- 核心主线 2：Chat / Inference / Knowledge
- 扩展主线：Workspace / Agent / Gateway / Heartbeat / CUA / MCP

其中扩展主线在未闭环前统一按 experimental 处理，不再与核心主线同级争夺实现优先级。

## 当前架构状态判断

### 1. 后端：大型单体 FastAPI，功能域过多但部分子系统成熟度较高

后端当前是单体 FastAPI 架构，通过 router 拼装多个功能域。这种结构在现阶段可以接受，但功能域已经较多，继续无节制扩展会让维护成本和耦合度迅速上升。

当前后端中最成熟的两块是：

- `Training` 子系统  
  证据：`server/core/training_state.py:81`、`server/core/training_queue.py:76`、`server/api/training.py:38`
- `Inference / Chat Context` 子系统  
  证据：`server/api/inference/routes.py:107`、`server/api/inference/routes.py:189`、`server/api/inference/routes.py:220`

这两条链路体现出了真实工程能力，而不是单纯的接口堆叠。

### 2. 前端：平台型 UI 已成形，但状态模型开始膨胀

前端采用 `React + TypeScript + Zustand + Ant Design + Framer Motion`，应用壳层已经比较完整，说明项目不是 demo 级前端。

但当前前端已有两个值得警惕的信号：

- `Chat` 已经演变成最复杂的业务中心  
  证据：`client/src/pages/ChatNew.tsx:202`
- 状态管理出现“双中心”重叠  
  证据：`client/src/store/appStore.ts:14`、`client/src/store/chatStore.ts:15`

这意味着前端后续若不收敛状态中心，维护成本会持续抬升。

### 3. 当前最大问题：不是缺功能，而是边界失控

当前架构层面有四类共性风险：

#### 产品边界扩散过快

一个项目里同时承担 `Finetune`、`Chat playground`、`Agent platform`、`Workspace`、`Gateway`、`Heartbeat`、`CUA/MCP` 等多条方向，会导致代码路径不断增加，但真正打通的功能闭环反而越来越少。

#### 新旧实现并存

项目中存在明显的新旧实现叠加现象，例如：

- memory old/new
- knowledge stub/real
- appStore/chatStore 职责重叠
- 某些兼容路由仍然保留

这会直接导致同一功能出现两套入口，开发者容易改错文件，前后端契约也更容易漂移。

#### 默认配置漂移

默认配置缺乏统一来源，例如默认端口不一致：

- 后端默认 `8001`：`server/core/config.py:28`
- 前端默认 `8000`：`client/src/services/api.ts:8`
- `client/src/App.tsx:194`

这类问题虽不一定立刻报错，但会直接影响开发体验和团队认知一致性。

#### UI 成熟度高于后端真实能力

尤其在以下领域较为明显：

- Knowledge
- Branching
- CUA 录制
- Workspace

这类模块最容易形成“看起来可用，实际未闭环”的伪实现。

## 架构诊断结论

### 1. 项目当前不是“没做出来”，而是“做得很大，但主闭环不够实”

项目已经具备以下优点：

1. 训练和推理主链路存在真实工程基础。
2. 前端整体具备平台型 UI 结构，而非演示性页面集合。
3. 很多模块不是纯占位，而是有明确的系统设计方向。
4. 项目整体目标已经不是单点工具，而是平台化能力组合。

但当前也存在以下核心问题：

1. 产品边界扩张过快。
2. 新旧实现重叠。
3. 部分能力只有 UI 或接口壳，没有真实数据闭环。
4. 某些模块看似完成，实际只完成了 40% 到 60%。

### 2. 当前整改的核心不是“继续加”，而是“先收”

下一阶段整改必须围绕三个目标展开：

1. 明确每个领域的唯一真实入口。
2. 识别并消灭伪实现。
3. 把平台能力收敛成真实可用闭环。

### 3. canonical 原则

后续所有整改均以以下原则为准：

- chat 领域只能有一套真实 session storage，主聊天系统的数据模型是唯一事实源。
- workspace 领域只能有一套元数据持久化模型，不能依赖仅存在内存中的状态。
- knowledge 领域只能保留一套路由实现，避免 stub 与真实实现并存。
- 前后端默认配置必须有唯一来源，避免在多个页面或服务中硬编码不同默认值。

### 4. 伪实现判定标准

本文档统一将以下情况定义为“伪实现”：

- 前端有入口或按钮，但后端没有对应接口。
- 后端接口返回 `success`，但没有真实状态变化。
- UI 显示操作成功，但刷新页面后无法验证状态仍然成立。

## 关键问题分级与证据

以下问题按照影响程度分级，统一采用“现状 -> 风险/影响 -> 架构决策 -> 整改动作”的结构描述。

### P0-1. Chat Branch 存储割裂

**现状**

前端 `ChatBranchManager` 请求的是一套聊天树/分支接口：

- `/api/chat/{sessionId}/tree`
- `/api/chat/{sessionId}/branches`

但后端 `server/api/chat_branch.py` 维护的是独立文件存储：

- `data/chat/session_{session_id}.json`
- `data/chat/branches_{session_id}.json`

主聊天系统 `server/api/chat/session.py` 使用的却是：

- `data/sessions/{session_id}.json`

证据：

- `client/src/components/ChatBranchManager.tsx:54`
- `client/src/components/ChatBranchManager.tsx:78`
- `server/api/chat_branch.py:16`
- `server/api/chat_branch.py:62`
- `server/api/chat/session.py:105`
- `server/api/chat/session.py:176`

**风险 / 影响**

- 分支系统无法读取主聊天系统的真实会话数据。
- 用户看不到真实聊天树。
- 分支创建、读取、切换都无法基于真实会话工作。
- 这是“UI 有了、接口也有了、底层却不通”的典型断裂。

**架构决策**

将主聊天 session 存储定义为 chat 领域唯一事实源。`chat_branch` 不再自行维护独立 session 文件。

**整改动作**

- 废弃 `data/chat/session_{id}.json` 与 `data/chat/branches_{id}.json`
- 将 branch/tree 元数据挂接到主 session 存储体系
- 调整 branch API 只读取和写入主会话存储

### P0-2. Action Recorder 缺失接口

**现状**

前端 `ActionRecorder` 调用了以下接口：

- `POST /cua/record/save`
- `POST /cua/record/load`
- `DELETE /cua/record/actions`

后端实际存在的接口是：

- `POST /cua/record/action`
- `GET /cua/record/actions`
- `GET /cua/record/files`
- `POST /cua/record/play`

证据：

- `client/src/pages/ActionRecorder.tsx:153`
- `client/src/pages/ActionRecorder.tsx:165`
- `client/src/pages/ActionRecorder.tsx:180`
- `server/api/cua.py:630`
- `server/api/cua.py:665`
- `server/api/cua.py:683`
- `server/api/cua.py:707`

**风险 / 影响**

- 用户无法保存录制动作。
- 无法加载动作文件。
- 无法清空动作集。
- 前端提示会误导用户，以为功能正常。

**架构决策**

在当前阶段，不允许“有按钮没后端”。

**整改动作**

- 若 CUA 不是当前主线，先隐藏或禁用 save/load/clear 相关操作。
- 若 CUA 属于后续重点，补齐完整保存、加载、清空协议。
- 成功提示必须在真实状态变化后再展示。

### P1-3. Branch Merge 假实现

**现状**

`/chat/{session_id}/merge-branch/{branch_id}` 仅校验 branch 是否存在，然后直接返回成功，没有真正合并消息、节点或上下文。

证据：

- `server/api/chat_branch.py:173`
- `client/src/components/ChatBranchManager.tsx:223`

**风险 / 影响**

- 用户会误以为分支已合并。
- 实际数据没有变化。
- 属于最危险的“成功提示但无副作用”实现。

**架构决策**

没有真实数据变化时，不允许返回成功。

**整改动作**

- 若短期不实现真实 merge，立即移除或禁用“合并分支”按钮。
- 若保留功能，先定义简单可解释的 merge 语义：
  - 只支持将某 branch 后续节点挂接到当前主链末端
  - 保留 `merge_from`、`merge_at`
  - 暂不支持冲突处理

### P1-4. Workspace 元数据不持久化

**现状**

`server/api/workspace.py` 通过全局内存字典保存 workspace 元数据：

- `workspaces: dict[str, dict[str, Any]] = {}`

证据：

- `server/api/workspace.py:19`
- `server/api/workspace.py:46`
- `server/api/workspace.py:71`
- `server/api/workspace.py:143`
- `client/src/pages/WorkspaceManager.tsx:43`

**风险 / 影响**

- 服务重启后 workspace 列表丢失。
- 用户体验表现为“系统失忆”。
- workspace 的存储模型未闭环。

**架构决策**

workspace 必须成为可重启恢复的持久化对象，而不是临时内存状态。

**整改动作**

- 建立统一的 workspace 元数据持久化模型，至少包含：
  - `workspace_id`
  - `name`
  - `description`
  - `created_at`
  - `updated_at`
  - `document_count`
  - `vector_collection_name`
  - `status`
- 启动时重建必要的内存索引
- 删除 workspace 时同步清理向量集合与元数据

### P1-5. Knowledge stub 误导维护

**现状**

项目同时存在：

- `server/api/knowledge.py`
- `server/api/knowledge/routes.py`

其中前者是 stub，后者才是真实实现。

证据：

- `server/api/knowledge.py:29`
- `server/api/knowledge.py:36`
- `server/api/knowledge.py:42`
- `server/api/knowledge.py:48`
- `server/api/knowledge/routes.py:125`
- `server/api/knowledge/routes.py:244`
- `server/api/knowledge/routes.py:281`
- `server/api/__init__.py:14`
- `server/api/knowledge/__init__.py:5`

**风险 / 影响**

- 新维护者容易改错文件。
- 会误判 knowledge 仍是空壳。
- 这是迁移遗留未清理带来的典型架构噪音。

**架构决策**

knowledge 领域只能保留一个正式入口。

**整改动作**

- 优先直接删除 `server/api/knowledge.py`
- 若短期不能删除，则至少重命名并标明废弃状态
- 保证文档、导出与引用只指向真实实现

### P2-6. Training WebSocket 连接滞留风险

**现状**

`TrainingWebSocketManager` 虽有超时与心跳设计，但异常断开后的连接清理偏保守，且混用了 `threading lock` 与 `asyncio lock`。

证据：

- `server/api/training.py:38`
- `server/api/training.py:50`
- `server/api/training.py:312`

**风险 / 影响**

- 高并发或频繁重连时可能积累 stale connection。
- 短期不一定出故障，但后续会恶化。

**架构决策**

训练进度连接管理需要更明确的生命周期清理与并发模型边界。

**整改动作**

- 优化断线回收逻辑
- 明确连接状态与清理时机
- 收敛锁模型，减少混用带来的维护风险

### P2-7. Gateway / Heartbeat 乐观 success

**现状**

前端页面较早显示 success message，而后端某些接口默认返回 `success: true`，未必对应真实有效的业务变化。

证据：

- `client/src/pages/GatewayPage.tsx:111`
- `client/src/pages/GatewayPage.tsx:137`
- `client/src/pages/HeartbeatPage.tsx:117`
- `server/api/heartbeat.py:94`
- `server/api/gateway_api/routes.py:96`

**风险 / 影响**

- 用户看到“操作成功”，但未必创建了有效绑定或任务。
- 属于体验层面的伪实现与半闭环。

**架构决策**

success 必须依赖后端真实验证结果，不能作为默认出口。

**整改动作**

- 将成功提示绑定到真实结果
- 调整接口响应结构，返回真实业务结果而不只是布尔值
- 对无真实副作用的操作明确提示未实现或实验态

### P2-8. 默认配置与状态源漂移

**现状**

默认端口与状态中心存在漂移：

- 后端默认端口 `8001`
- 前端默认端口 `8000`
- `appStore` 与 `chatStore` 职责重叠

证据：

- `server/core/config.py:28`
- `client/src/services/api.ts:8`
- `client/src/App.tsx:194`
- `client/src/store/appStore.ts:26`
- `client/src/store/chatStore.ts:61`

**风险 / 影响**

- 本地启动体验不一致。
- 团队难以确认 canonical 配置来源。
- chat 状态存在双份来源，后续更难维护。

**架构决策**

配置与状态中心必须有唯一官方路径。

**整改动作**

- 统一前后端默认配置来源
- 减少前端多处硬编码 backend URL
- 将 `chatStore` 收敛为 chat 领域唯一状态中心
- `appStore` 只保留全局壳层状态

## 整改路线图与实施计划

### 整改目标

整改的总体目标不是继续堆功能，而是先恢复系统可信度、补齐数据闭环、降低架构噪音，并最终完成产品收口。

### P0：先恢复系统可信度

目标：

- 前端每个操作都有真实后端支撑
- 后端 success 一定对应真实状态变化
- 用户不会再被“假成功”误导

本批次任务：

1. Chat Branch 存储统一
2. Action Recorder 缺失接口处理
3. 移除或禁用假 success
4. Workspace 元数据持久化

验收标准：

- 创建会话后 branch API 读取的是同一份真实 session 数据
- Action Recorder 页面不再触发 404 型操作
- 成功提示均可通过刷新页面验证
- workspace 重启后仍可恢复

### P1：补齐数据闭环

目标：

- 系统重启后关键数据不丢
- 存储与 UI 认知一致
- 核心状态可被追溯和验证

本批次任务：

1. Workspace 元数据持久化模型落地
2. Branch Merge 真实实现或正式下线
3. 统一 chat 领域唯一事实源

验收标准：

- workspace 列表与实际向量集合一致
- merge 后数据刷新仍存在
- chat 相关状态只有一套真实写入路径

### P2：减少架构噪音，建立 canonical path

目标：

- 同一功能只有一条官方路径
- 降低维护误改概率
- 为后续重构腾出空间

本批次任务：

1. 删除或废弃 knowledge stub
2. 统一前后端默认配置来源
3. 收敛 chat state/store

验收标准：

- knowledge 只保留一个正式实现入口
- 新开发者按默认命令能直接连通系统
- chat 页面所有消息、会话、流式状态只来自一个 store

### P3：模块分层与产品收口

目标：

- 核心主线优先闭环
- 扩展能力降级为 experimental
- 避免非核心模块与主链路争夺资源

本批次任务：

1. 将 `Gateway / Heartbeat / CUA / MCP` 标记为 experimental
2. 给 experimental 能力下沉入口并加标识
3. 补契约测试 / 端到端验证
4. 清理 legacy alias 与兼容层

验收标准：

- experimental 模块不再被作为一线主能力承诺
- 团队能明确区分核心能力与试验能力
- 新增功能必须满足伪实现红线

### 推荐实施顺序

第一批先做：

1. Chat Branch 存储统一
2. Action Recorder 缺失接口或隐藏无效按钮
3. 移除假 success
4. Workspace 持久化

第二批紧接着做：

5. 删除 knowledge stub
6. 统一默认配置
7. 收敛 chat state/store

第三批最后做：

8. Gateway / Heartbeat / CUA / MCP 重新分级
9. 补契约测试
10. 清理 legacy alias / 兼容层

## 架构治理硬规则

为防止后续再次出现边界失控、伪实现和事实源分裂，后续功能演进必须遵守以下硬规则。

### 规则 1：一个领域只能有一个 canonical 存储模型

例如：

- chat session 只能有一套真实 session storage
- workspace 只能有一套元数据持久化模型
- knowledge 只能有一套路由实现

### 规则 2：任何 success 都必须可刷新验证

接口返回 success 时，必须能够通过刷新页面、重新查询或重启服务验证状态仍然成立。

### 规则 3：所有 experimental 功能默认不进入主承诺

未闭环前的能力可以保留探索，但必须在 UI 和文档中明确标识，不与主链路同级承诺。

### 规则 4：优先做闭环，不优先做展示面

后续实现中，应优先完成少数模块的真实闭环，而不是让更多模块停留在 50% 左右的半实现状态。

### 规则 5：新增功能必须通过“伪实现红线”

任何新功能上线前至少满足以下条件：

1. 前端操作有真实后端接口。
2. 后端返回 success 必须对应真实状态变化。
3. 刷新页面后状态仍可验证。
4. 至少有一条端到端测试或契约测试。

## 维护说明

- 本文档为项目架构诊断、收口决策与整改路线图的唯一持续维护版本。
- `架构.txt` 与 `优化.txt` 为历史草稿来源，后续不再继续扩写。
- 若后续需要归档，可将两份 TXT 草稿移入专门的归档目录；在归档前，默认保留原文件供追溯参考。
- `.trae/documents/` 与 `.trae/specs/` 下的历史方案稿已通过归档说明标记为“仅供追溯，不代表当前 canonical 实现”。

## 实施状态更新（2026-04-08）

以下整改项已完成并落地到代码主干：

- `Chat Branch` 已切回主 session storage，不再维护 `data/chat/session_{id}.json` 与 `data/chat/branches_{id}.json` 这套割裂存储。
- `Action Recorder` 已补齐 `save/load/clear` 后端接口，前端不再调用不存在的录制 API。
- `Branch Merge` 已停止假成功返回；前端入口已禁用，后端以未实现语义返回错误。
- `Workspace` 元数据已落盘持久化，不再只存在内存字典中。
- `knowledge` stub 已清理，knowledge 领域只保留正式实现入口。
- 前后端默认配置已统一到同一来源，默认端口与后端地址不再漂移。
- chat 状态中心已收敛，`chatStore` 成为 chat 领域唯一状态中心。
- `Gateway / Heartbeat / CUA / MCP` 已在页面层明确标注为 experimental。
- `Gateway` 与 `Heartbeat` 的乐观 success 语义已收紧，失败会返回真实错误而不是默认成功。
- 设备与任务列表接口已补齐 canonical 字段，前端展示不再依赖猜测字段名。
- 前端通用 chat helper、历史测试脚本都已迁移到 `/chat/sessions...` canonical 路径。
- 旧 `Chat` compat 路由已移除：`POST /chat`、`GET /chat`、`GET /chat/{session_id}`、`DELETE /chat/{session_id}`、`POST /chat/{session_id}/messages`。
- 旧 `Training` 根别名 `GET /training` 已移除，训练状态统一通过 `GET /training/status` 暴露。

当前仍保留为后续阶段任务的项目：

- `Branch Merge` 的真实合并语义尚未实现，只完成了“停止误导”。
- `Training WebSocket` 的更深层生命周期治理仍可继续优化，但当前未阻塞主链路。
- `.trae/documents/` 等历史设计稿仍保留原始表述，尚未整体做归档级清理。
