# Finetune Platform 架构重构实施计划

## 基于分层意图架构 × 混合模型架构 × ReAct 架构的融合设计

**版本**: 3.0  
**制定日期**: 2026 年 3 月 26 日  
**预计周期**: 10-14 周  
**优先级**: P0 → P1 → P2 → P3

---

## 📋 目录

1. [项目概述](#项目概述)
2. [现状分析](#现状分析)
3. [目标架构](#目标架构)
4. [ReAct 架构详解](#react 架构详解)
5. [重构方案](#重构方案)
6. [实施路线图](#实施路线图)
7. [风险与应对](#风险与应对)
8. [验收标准](#验收标准)
9. [附录](#附录)

---

## 项目概述

### 背景

《优化.txt》文档描述了**分层意图架构 (Layered Intent Architecture)** 与 **混合模型架构 (Hybrid Model Architecture)** 的融合设计，代表了当前 AI 工程化的最高水平。

**ReAct 架构** (Reasoning + Acting) 是谷歌在 ICLR 2023 发表的论文提出的 Agent 架构，核心思想是**推理痕迹 (Reasoning Traces)** 与 **任务特定动作 (Task-specific Actions)** 交错执行。实验表明：
- 在 HotpotQA 和 FEVER 基准上超越 CoT（思维链）方法
- 在 ALFWorld 决策任务上比模仿学习/强化学习方法高 **34%** 绝对成功率
- 在 WebShop 购物任务上高 **10%** 绝对成功率

**核心优势**：
- ✅ 克服幻觉和错误传播
- ✅ 更高的人类可解释性
- ✅ 支持人类对齐修正（通过修改思考轨迹）
- ✅ 仅需 1-2 个上下文示例即可取得优异表现

### 目标

将 Finetune Platform 重构为**混合模型驱动的分层意图系统 + ReAct 自主 Agent**，实现：

| 目标 | 当前状态 | 目标状态 | 提升 |
|------|----------|----------|------|
| 简单请求延迟 | ~300ms | ~40ms | **87% 降低** |
| Token 成本 | 100% | 20% | **80% 降低** |
| 长文本上限 | 32K | 1M+ | **30 倍提升** |
| 安全拦截率 | 80% | 99% | **24% 提升** |
| 复杂推理准确率 | 85% | 96% | **13% 提升** |
| Agent 任务完成率 | 60% | **90%+** | **50% 提升** |
| 任务可解释性 | 低 | **高** | **质的飞跃** |

### 范围

**纳入重构的模块**：
- ✅ 安全模块 (`server/security/`)
- ✅ 核心模块 (`server/core/`)
- ✅ AI 网关 (`server/ai/`)
- ✅ Agent 模块 (`server/agent/`)

**保持不变的模块**：
- ❌ 前端应用 (`client/`) - 仅接口适配
- ❌ 数据库 Schema - 向后兼容
- ❌ 现有 API 接口 - 保持兼容

---

## 现状分析

### 当前架构评估

```
┌─────────────────────────────────────────────────────────────┐
│                    Finetune Platform 当前架构                │
├─────────────────────────────────────────────────────────────┤
│  前端层：React 18 + TypeScript + Ant Design                 │
│       ↓ HTTP/WebSocket                                      │
│  API 层：FastAPI (38 个路由模块)                              │
│       ↓                                                     │
│  核心层：Config/Logging/State/Queue/EventBus/DI            │
│       ↓                                                     │
│  领域层：Agent / Memory / RAG / Security / AI Gateway      │
│       ↓                                                     │
│  基础设施：PyTorch 2.6 / Transformers / SQLite             │
└─────────────────────────────────────────────────────────────┘
```

### 与目标架构差距

| 层级 | 目标架构要求 | 当前状态 | 差距 | 优先级 |
|------|-------------|----------|------|--------|
| **L1 网关过滤层** | 安检 + 清洗 + 限流 + WAF | ⚠️ 有基础安全模块 | ❌ 缺少 WAF 规则引擎、脏数据过滤 | P0 |
| **L2 意图路由层** | 意图分类 + 动态门控 + 熵值计算 | ⚠️ 有 intent_detector.py | ❌ 缺少动态路由决策、成本优化 | P0 |
| **L3 核心处理层** | Chat/Flow/ReAct 三模式 + 混合模型 | ⚠️ 有 Chat 和 Agent | ❌ 缺少 Flow 模式、**ReAct 架构**、混合模型支持 | P1 |
| **L4 执行守卫层** | 流式检测 + 深度审计 + 策略引擎 | ⚠️ 有 prompt_security.py | ❌ 缺少 OPA 策略引擎、流式切断 | P1 |

### 核心问题

1. **缺少分层路由**：所有请求走同一通道，无法降本增效
2. **缺少混合模型**：仅支持纯 Transformer，长文本效率低
3. **安全机制不足**：缺少流式切断、深度语义审计
4. **缺少工作流引擎**：确定性任务无法走高效硬编码路径

---

## 目标架构

### 融合架构设计

```
                              用户输入
                                 ↓
┌─────────────────────────────────────────────────────────────┐
│  L1: 混合感知网关                                            │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ WAF 规则   │ │脏数据过滤  │ │ 速率限制   │ │Prompt 注入 │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
└─────────────────────────────────────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────┐
│  L2: 动态智能路由 (Hybrid Router)                            │
│                    混合分析引擎                              │
│         ┌───────────┬───────────┬───────────┐               │
│         ↓           ↓           ↓           ↓               │
│    简单请求     复杂请求     超长文档    工具调用             │
│   (System 1)   (System 2)   (长程记忆)    (ReAct)            │
└─────────────────────────────────────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────┐
│  L3: 混合模型执行集群                                        │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │Chat 模式   │ │Flow 模式   │ │ReAct 模式  │ │混合模型   │   │
│  │(通用对话)  │ │(硬编码)   │ │(谷歌架构) │ │(Transformer+Mamba)│
│  └───────────┘ └───────────┘ └─────┬─────┘ └───────────┘   │
│                                    │                        │
│                    ┌───────────────┴───────────────┐        │
│                    │  ReAct 核心循环                │        │
│                    │  Thought → Action → Observation│        │
│                    └───────────────────────────────┘        │
└─────────────────────────────────────────────────────────────┘
                                 ↓
┌─────────────────────────────────────────────────────────────┐
│  L4: 混合守卫层                                              │
│         ┌───────────────────┬───────────────────┐           │
│         ↓                   ↓                   ↓           │
│    SSM 快流           Transformer 慢流        决策          │
│   (实时流检测)        (深度语义审计)       (通过/阻断)       │
└─────────────────────────────────────────────────────────────┘
                                 ↓
                            输出 / 阻断
```

### 核心运作机制

#### 1. L2 动态门控 (Dynamic Gating)

| 输入特征 | 决策逻辑 | 路由路径 |
|----------|----------|----------|
| 熵值 < 3.5, 复杂度 < 0.3 | 简单请求 | FAST (Mamba/小模型) |
| 熵值 > 4.0, 复杂度 > 0.5 | 复杂推理 | SLOW (Transformer) |
| Token 数 > 8000 | 长上下文 | LONG (Hybrid) |
| 意图置信度 < 0.7 | 意图不明确 | REACT (Agent) |
| 匹配预定义模式 | 确定性任务 | FLOW (硬编码) |

#### 2. L3 按需变形

| 场景 | 模型选择 | 响应时间 | 成本 |
|------|----------|----------|------|
| 日常问答 | Mamba 专家 | <100ms | 0.001 元/1K tokens |
| 复杂调试 | Transformer 专家 | ~1000ms | 0.01 元/1K tokens |
| 长文档分析 | 混合架构模型 | ~300ms | 0.005 元/1K tokens |

## ReAct 架构详解

### 核心原理

**ReAct** (Reasoning + Acting) 的核心是**协同效应**：推理帮助规划行动，行动获取的信息反过来增强推理。

```
┌─────────────────────────────────────────────────────────────┐
│                    ReAct 协同效应                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   推理 (Reasoning)              行动 (Acting)               │
│   ┌─────────────┐              ┌─────────────┐             │
│   │ 诱导计划    │─────────────▶│ 执行动作    │             │
│   │ 跟踪状态    │◀─────────────│ 获取反馈    │             │
│   │ 更新策略    │─────────────▶│ 异常处理    │             │
│   │ 反思修正    │◀─────────────│ 环境响应    │             │
│   └─────────────┘              └─────────────┘             │
│                                                             │
│   协同效应：1+1 > 2                                         │
│   - 推理使行动有目的性                                       │
│   - 行动使推理有事实依据                                     │
└─────────────────────────────────────────────────────────────┘
```

### Thought-Action-Observation 循环机制

#### 标准格式规范

| 步骤 | 格式 | 说明 | 示例 |
|------|------|------|------|
| **Thought** | `Thought {n}: {free_form_text}` | 推理痕迹，自由文本 | `Thought 1: 我需要先搜索 Python 教程` |
| **Action** | `Action {n}: {tool_name}[{tool_input}]` | 工具调用，方括号参数 | `Action 1: Search[Python 教程 2026]` |
| **Observation** | `Observation {n}: {tool_output}` | 工具返回结果（环境注入） | `Observation 1: 找到 10 个教程...` |

#### 循环流程图

```
┌─────────────┐
│   Question  │ 用户输入
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Prompt = Few-Shot Examples + Question  │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────┐
│ Thought 1   │ 推理：分析问题，规划下一步
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Action 1   │ 行动：调用工具
└──────┬──────┘
       │
       ▼
┌─────────────┐  Yes  ┌─────────────┐
│ Is Finish?  │──────▶│ Final Answer│ 输出答案
└──────┬──────┘       └─────────────┘
       │ No
       ▼
┌─────────────┐
│ Observation │ 环境反馈（工具返回）
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ Thought 2   │ 基于反馈继续推理
└──────┬──────┘
       │
       ▼
    (循环继续...)
```

### Few-Shot Prompt 模板

```python
REACT_SYSTEM_PROMPT = """
你是一个智能助手，使用 ReAct 模式解决问题。你可以使用以下工具：

{tool_definitions}

使用以下格式：

Question: 输入问题
Thought: 你应该总是先思考要做什么
Action: 要采取的行动，应该是 [{tool_names}] 之一
Action Input: 行动的输入
Observation: 行动的结果
... (这个 Thought/Action/Action Input/Observation 循环可以重复 N 次)
Thought: 我现在知道最终答案
Final Answer: 对输入问题的最终回答

示例 1:
Question: Python 的作者是谁？
Thought: 我需要搜索 Python 编程语言的历史
Action: Search
Action Input: Python 编程语言 创始人
Observation: Python 是由 Guido van Rossum 创建的，首次发布于 1991 年。
Thought: 我现在知道了答案
Final Answer: Python 是由 Guido van Rossum 创建的。

示例 2:
Question: 中国人口是多少？乘以 2 是多少？
Thought: 我需要先查找中国人口
Action: Search
Action Input: 中国 人口 2026
Observation: 中国人口约为 14.1 亿。
Thought: 现在我需要计算 14.1 亿乘以 2
Action: Calculator
Action Input: 1410000000 * 2
Observation: 2820000000
Thought: 我现在知道了最终答案
Final Answer: 中国人口约为 14.1 亿，乘以 2 是 28.2 亿。

现在，请解决以下问题：

Question: {input}
{agent_scratchpad}
"""
```

### 工具注册表设计

```python
# server/core/tool_registry.py

from typing import Any, Callable, Dict, List, Optional
from dataclasses import dataclass, field
from pydantic import BaseModel, Field

@dataclass
class ToolDefinition:
    """工具定义"""
    name: str                              # 工具名称
    description: str                       # 工具描述
    func: Callable[[str], str]            # 工具函数
    parameters: Optional[Dict] = None     # 参数 Schema
    examples: List[str] = field(default_factory=list)  # 使用示例

class ToolRegistry:
    """工具注册表"""
    
    def __init__(self):
        self.tools: Dict[str, ToolDefinition] = {}
    
    def register(
        self,
        name: str,
        description: str,
        parameters: Optional[Dict] = None
    ) -> Callable:
        """装饰器方式注册工具"""
        def decorator(func: Callable) -> Callable:
            self.tools[name] = ToolDefinition(
                name=name,
                description=description,
                func=func,
                parameters=parameters or {},
                examples=[]
            )
            return func
        return decorator
    
    def get_tool(self, name: str) -> Optional[ToolDefinition]:
        """获取工具"""
        return self.tools.get(name)
    
    def list_tools(self) -> List[Dict]:
        """列出所有工具"""
        return [
            {
                "name": tool.name,
                "description": tool.description,
                "parameters": tool.parameters
            }
            for tool in self.tools.values()
        ]
    
    def execute(self, name: str, input_data: str) -> str:
        """执行工具"""
        if name not in self.tools:
            return f"Error: Unknown tool '{name}'"
        
        try:
            tool = self.tools[name]
            result = tool.func(input_data)
            return str(result)
        except Exception as e:
            return f"Error executing {name}: {str(e)}"

# 使用示例
registry = ToolRegistry()

@registry.register(
    name="Search",
    description="搜索网络信息",
    parameters={"type": "string", "format": "query"}
)
def search(query: str) -> str:
    """搜索函数"""
    # 实际实现会调用搜索引擎 API
    return f"搜索结果：{query}"

@registry.register(
    name="Calculator",
    description="执行数学计算",
    parameters={"type": "string", "format": "expression"}
)
def calculate(expression: str) -> str:
    """计算函数"""
    try:
        result = eval(expression)
        return str(result)
    except Exception as e:
        return f"计算错误：{str(e)}"

@registry.register(
    name="FileRead",
    description="读取文件内容",
    parameters={"type": "string", "format": "file_path"}
)
def file_read(path: str) -> str:
    """读取文件"""
    with open(path, 'r', encoding='utf-8') as f:
        return f.read()
```

### ReAct 引擎实现

```python
# server/core/react_engine.py

from typing import Dict, List, Optional, Tuple
from dataclasses import dataclass
import re
import asyncio

@dataclass
class ReActStep:
    """ReAct 步骤"""
    step_number: int
    thought: str
    action_name: str
    action_input: str
    observation: Optional[str] = None

@dataclass
class ReActResult:
    """ReAct 执行结果"""
    success: bool
    answer: Optional[str]
    trajectory: List[ReActStep]
    error: Optional[str] = None
    total_steps: int = 0
    total_tokens: int = 0

class ReActEngine:
    """
    ReAct 引擎
    
    实现 Reasoning + Acting 循环
    """
    
    # 默认配置
    DEFAULT_CONFIG = {
        "max_iterations": 10,        # 最大迭代次数
        "early_stopping": True,      # 早停
        "handle_parsing_errors": True,  # 处理解析错误
        "verbose": True,             # 详细日志
    }
    
    def __init__(
        self,
        llm,
        tool_registry: ToolRegistry,
        prompt_template: str = None,
        config: Dict = None
    ):
        self.llm = llm
        self.tools = tool_registry
        self.prompt_template = prompt_template or self._default_prompt()
        self.config = {**self.DEFAULT_CONFIG, **(config or {})}
    
    def _default_prompt(self) -> str:
        """默认 Prompt 模板"""
        return REACT_SYSTEM_PROMPT
    
    async def execute(
        self,
        question: str,
        context: Dict = None
    ) -> ReActResult:
        """
        执行 ReAct 循环
        
        Args:
            question: 用户问题
            context: 上下文信息
        
        Returns:
            ReActResult: 执行结果
        """
        trajectory: List[ReActStep] = []
        agent_scratchpad = ""
        
        for step_num in range(1, self.config["max_iterations"] + 1):
            # 1. 构建完整 Prompt
            full_prompt = self.prompt_template.format(
                tool_definitions=self._get_tool_definitions(),
                tool_names=self._get_tool_names(),
                input=question,
                agent_scratchpad=agent_scratchpad
            )
            
            # 2. 调用 LLM 生成 Thought + Action
            response = await self.llm.generate(full_prompt)
            
            # 3. 解析 Thought
            thought = self._parse_thought(response)
            
            # 4. 解析 Action
            action_name, action_input = self._parse_action(response)
            
            # 5. 创建步骤记录
            step = ReActStep(
                step_number=step_num,
                thought=thought,
                action_name=action_name,
                action_input=action_input
            )
            
            # 6. 检查终止条件
            if action_name == "Finish" or action_name == "Final Answer":
                return ReActResult(
                    success=True,
                    answer=action_input,
                    trajectory=trajectory,
                    total_steps=len(trajectory)
                )
            
            # 7. 执行工具调用
            observation = await self._execute_action(action_name, action_input)
            step.observation = observation
            
            # 8. 更新轨迹
            trajectory.append(step)
            
            # 9. 更新 scratchpad
            agent_scratchpad += self._format_step(step)
            
            # 10. 检查错误
            if self._is_error(observation):
                if self.config["handle_parsing_errors"]:
                    agent_scratchpad += "\nThought: 让我尝试另一种方法...\n"
                else:
                    return ReActResult(
                        success=False,
                        answer=None,
                        trajectory=trajectory,
                        error=observation,
                        total_steps=len(trajectory)
                    )
        
        # 达到最大迭代次数
        return ReActResult(
            success=False,
            answer=None,
            trajectory=trajectory,
            error="Maximum iterations reached",
            total_steps=len(trajectory)
        )
    
    def _parse_thought(self, response: str) -> str:
        """解析 Thought"""
        match = re.search(r'Thought\s*\d*:\s*(.*?)(?=Action|$)', response, re.DOTALL)
        if match:
            return match.group(1).strip()
        return response.strip()
    
    def _parse_action(self, response: str) -> Tuple[str, str]:
        """解析 Action"""
        # 匹配 Action: ToolName[Input] 格式
        action_pattern = r'Action\s*\d*:\s*(\w+)\s*\[([^\]]*)\]'
        match = re.search(action_pattern, response)
        
        if match:
            return match.group(1), match.group(2)
        
        # 匹配 Final Answer
        final_pattern = r'(?:Final Answer|Thought:\s*I now know the final answer)[:\s]*(.*)'
        final_match = re.search(final_pattern, response, re.DOTALL)
        
        if final_match:
            return "Finish", final_match.group(1).strip()
        
        return "Unknown", response
    
    async def _execute_action(self, name: str, input_data: str) -> str:
        """执行动作"""
        return self.tools.execute(name, input_data)
    
    def _format_step(self, step: ReActStep) -> str:
        """格式化步骤"""
        return (
            f"Thought {step.step_number}: {step.thought}\n"
            f"Action {step.step_number}: {step.action_name}[{step.action_input}]\n"
            f"Observation {step.step_number}: {step.observation}\n"
        )
    
    def _is_error(self, observation: str) -> bool:
        """检查是否为错误"""
        error_keywords = ["error", "failed", "exception", "not found"]
        return any(kw in observation.lower() for kw in error_keywords)
    
    def _get_tool_definitions(self) -> str:
        """获取工具定义"""
        tools = self.tools.list_tools()
        return "\n".join(
            f"- {t['name']}: {t['description']}"
            for t in tools
        )
    
    def _get_tool_names(self) -> str:
        """获取工具名称列表"""
        tools = self.tools.list_tools()
        return ", ".join(t["name"] for t in tools)
```

### 终止条件判断

```python
def check_termination(
    action_name: str,
    action_input: str,
    step: int,
    max_iterations: int,
    trajectory: List[ReActStep]
) -> Tuple[bool, Optional[str]]:
    """
    检查终止条件
    
    Returns:
        (is_terminated, final_answer)
    """
    # 条件 1: Finish 动作
    if action_name in ["Finish", "Final Answer"]:
        return True, action_input
    
    # 条件 2: 达到最大迭代次数
    if step >= max_iterations:
        return True, "Maximum iterations reached."
    
    # 条件 3: 重复动作检测（防止死循环）
    if is_duplicate_action(action_name, action_input, trajectory):
        return True, "Detected duplicate actions."
    
    # 条件 4: 工具返回错误且无法恢复
    if action_name != "Unknown":
        last_observation = trajectory[-1].observation if trajectory else None
        if last_observation and "error" in last_observation.lower():
            # 检查是否有替代工具
            if not has_alternative_tool(action_name):
                return True, f"Tool {action_name} failed with no alternative."
    
    return False, None

def is_duplicate_action(
    action_name: str,
    action_input: str,
    trajectory: List[ReActStep],
    threshold: int = 3
) -> bool:
    """检测重复动作"""
    if len(trajectory) < threshold:
        return False
    
    recent_actions = trajectory[-threshold:]
    return all(
        step.action_name == action_name and step.action_input == action_input
        for step in recent_actions
    )
```

### 错误处理机制

```python
class ReActErrorHandler:
    """ReAct 错误处理器"""
    
    ERROR_HANDLERS = {
        "format_error": "handle_format_error",
        "tool_error": "handle_tool_error",
        "timeout_error": "handle_timeout_error",
        "loop_error": "handle_loop_error",
    }
    
    def handle_format_error(self, response: str) -> str:
        """处理格式错误"""
        return (
            "Thought: 我需要使用正确的格式。\n"
            "Action: [工具名]\n"
            "Action Input: [输入]\n"
            "让我重新尝试..."
        )
    
    def handle_tool_error(self, tool_name: str, error: str) -> str:
        """处理工具错误"""
        return (
            f"Thought: {tool_name} 工具执行失败：{error}。\n"
            "让我尝试其他方法..."
        )
    
    def handle_timeout_error(self) -> str:
        """处理超时错误"""
        return (
            "Thought: 操作超时。\n"
            "让我尝试更简单的方法..."
        )
    
    def handle_loop_error(self, trajectory: List[ReActStep]) -> str:
        """处理循环错误"""
        return (
            "Thought: 我似乎陷入了循环。\n"
            "让我重新思考这个问题..."
        )
```

### 与 CoT 的区别

| 维度 | CoT（思维链） | ReAct（推理 + 行动） |
|------|--------------|---------------------|
| **知识来源** | 仅模型内部知识 | 内部知识 + 外部工具 |
| **幻觉问题** | 容易产生幻觉 | 通过工具调用减少幻觉 |
| **错误传播** | 错误会累积 | 可通过观察修正 |
| **可解释性** | 中等（推理链） | 高（推理 + 行动轨迹） |
| **任务范围** | 静态推理题 | 交互式决策 + 推理 |
| **示例需求** | 需要多个示例 | 1-2 个示例即可 |
| **外部交互** | ❌ 不支持 | ✅ 支持 |

### 性能基准

| 基准测试 | 指标 | ReAct 表现 | 提升 |
|----------|------|-----------|------|
| **HotpotQA** | 准确率 | 优于 CoT | 克服幻觉 |
| **FEVER** | 准确率 | 优于 CoT | 可交互验证 |
| **ALFWorld** | 成功率 | 63% | +34% vs IL/RL |
| **WebShop** | 成功率 | 42% | +10% vs IL/RL |

---

## 重构方案

### 模块设计清单

#### 阶段一：分层骨架

| 模块 | 文件路径 | 功能 | 行数预估 |
|------|----------|------|----------|
| WAF 规则引擎 | `server/security/waf_engine.py` | SQL 注入/XSS/Prompt 注入检测 | ~200 |
| 脏数据过滤器 | `server/core/content_filter.py` | 垃圾内容/低质量输入过滤 | ~150 |
| 网关中间件 | `server/security/gateway_middleware.py` | L1 统一请求入口 | ~200 |
| 熵值计算器 | `server/core/entropy_calculator.py` | 文本熵值/复杂度分析 | ~200 |
| 动态路由器 | `server/core/dynamic_router.py` | 基于熵值的路由决策 | ~250 |

#### 阶段二：混合基座

| 模块 | 文件路径 | 功能 | 行数预估 |
|------|----------|------|----------|
| Flow 引擎 | `server/core/flow_engine.py` | 硬编码工作流执行 | ~350 |
| **ReAct 引擎** | `server/core/react_engine.py` | **谷歌 ReAct 架构实现** | ~400 |
| **工具注册表** | `server/core/tool_registry.py` | **ReAct 工具管理** | ~250 |
| **推理轨迹记录** | `server/core/trajectory_logger.py` | **ReAct 轨迹记录/回放** | ~150 |
| 混合模型适配器 | `server/ai/hybrid_model_adapter.py` | Jamba/Falcon-H1 适配 | ~300 |
| 流式检测器 | `server/security/streaming_detector.py` | SSM 实时流检测 | ~200 |
| 语义审计器 | `server/security/semantic_auditor.py` | Transformer 深度审计 | ~300 |
| 策略引擎 | `server/security/policy_engine.py` | OPA 策略决策 | ~200 |

#### 阶段三：全链路自适应

| 模块 | 文件路径 | 功能 | 行数预估 |
|------|----------|------|----------|
| RL 路由器 | `server/core/rl_router.py` | 强化学习路由优化 | ~250 |
| 自我修正循环 | `server/core/self_correction.py` | System 1+2 协作 | ~200 |
| **ReAct 优化器** | `server/core/react_optimizer.py` | **ReAct 性能优化** | ~200 |

---

### 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| WAF 引擎 | 自研 (正则匹配) | 轻量、可定制、无额外依赖 |
| 动态路由 | 自研 (熵值计算) | 与业务深度集成 |
| 混合模型 | Jamba / Falcon-H1 | 成熟开源、256K 上下文 |
| 流式检测 | 自研 (滑动窗口) | 低延迟、可嵌入 |
| 策略引擎 | OPA (可选) / 自研 | 灵活、可降级 |
| RL 路由 | Q-Learning | 简单有效、易实现 |

### 依赖关系

```
阶段一 (P0)
├── waf_engine.py (无依赖)
├── content_filter.py (无依赖)
├── gateway_middleware.py (依赖 waf_engine, content_filter, rate_limiter)
├── entropy_calculator.py (无依赖)
└── dynamic_router.py (依赖 entropy_calculator)

阶段二 (P1)
├── flow_engine.py (无依赖)
├── hybrid_model_adapter.py (依赖 transformers)
├── streaming_detector.py (无依赖)
├── semantic_auditor.py (依赖 transformers)
└── policy_engine.py (无依赖)

阶段三 (P2)
├── rl_router.py (依赖 dynamic_router)
└── self_correction.py (依赖 hybrid_model_adapter, semantic_auditor)
```

---

## 实施路线图

### 总体时间线

```
2026 年 3 月 26 日                                    2026 年 5 月 15 日
     │───────────────│───────────────│───────────────│
     阶段一           阶段二           阶段三
     (分层骨架)       (混合基座)       (全链路自适应)
     2-3 周           3-4 周           2-3 周
```

### 阶段一：分层骨架 (2-3 周)

**目标**：实现 L1-L4 流程打通，获得初步收益

#### 任务分解

| ID | 任务 | 工时 | 前置 | 负责人 | 交付物 |
|----|------|------|------|--------|--------|
| P0-1 | WAF 规则引擎 | 3 天 | - | - | `waf_engine.py` + 单元测试 |
| P0-2 | 脏数据过滤器 | 2 天 | - | - | `content_filter.py` + 单元测试 |
| P0-3 | 熵值计算器 | 2 天 | - | - | `entropy_calculator.py` + 单元测试 |
| P0-4 | 动态路由器 | 4 天 | P0-3 | - | `dynamic_router.py` + 单元测试 |
| P0-5 | 网关中间件 | 3 天 | P0-1, P0-2 | - | `gateway_middleware.py` + 集成测试 |
| P0-6 | 集成测试 | 3 天 | P0-1~P0-5 | - | 测试报告 + 性能基准 |

#### 里程碑

- [ ] M1: WAF 规则引擎完成 (Day 3)
- [ ] M2: 动态路由器完成 (Day 9)
- [ ] M3: 网关中间件上线 (Day 12)
- [ ] M4: 阶段一验收 (Day 15)

#### 预期收益

- 80% 简单请求被正确路由到快速通道
- 恶意请求拦截率提升至 95%+
- 平均响应延迟降低 30%

---

### 阶段二：混合基座 (3-4 周)

**目标**：引入混合模型支持，增强执行和守卫能力，**实现 ReAct 架构**

#### 任务分解

| ID | 任务 | 工时 | 前置 | 负责人 | 交付物 |
|----|------|------|------|--------|--------|
| P1-1 | Flow 工作流引擎 | 5 天 | - | - | `flow_engine.py` + 单元测试 |
| **P1-2** | **ReAct 引擎** | **7 天** | **-** | **-** | **`react_engine.py` + 单元测试** |
| **P1-3** | **工具注册表** | **4 天** | **-** | **-** | **`tool_registry.py` + 单元测试** |
| **P1-4** | **推理轨迹记录器** | **3 天** | **P1-2** | **-** | **`trajectory_logger.py` + 单元测试** |
| P1-5 | 混合模型适配器 | 7 天 | - | - | `hybrid_model_adapter.py` + 单元测试 |
| P1-6 | 流式检测器 | 4 天 | - | - | `streaming_detector.py` + 单元测试 |
| P1-7 | 语义审计器 | 5 天 | - | - | `semantic_auditor.py` + 单元测试 |
| P1-8 | 策略引擎 | 3 天 | - | - | `policy_engine.py` + 单元测试 |
| P1-9 | 集成测试 | 4 天 | P1-1~P1-8 | - | 测试报告 + 性能基准 |

#### 里程碑

- [ ] M5: Flow 引擎完成 (Day 20)
- [ ] **M6: ReAct 引擎完成 (Day 27)**
- [ ] **M7: 工具注册表 + 轨迹记录完成 (Day 31)**
- [ ] M8: 混合模型适配器完成 (Day 34)
- [ ] M9: 流式检测 + 语义审计完成 (Day 38)
- [ ] M10: 阶段二验收 (Day 42)

#### 预期收益

- 支持 Jamba/Falcon-H1 等混合模型
- 长文本处理能力提升至 256K+ tokens
- 流式违规切断延迟 < 50ms
- **Agent 任务完成率提升至 80%+**
- **任务执行过程完全可解释、可追溯**

---

### 阶段三：全链路自适应 (2-3 周)

**目标**：实现强化学习路由和自我修正循环，**优化 ReAct 性能**

#### 任务分解

| ID | 任务 | 工时 | 前置 | 负责人 | 交付物 |
|----|------|------|------|--------|--------|
| P2-1 | 强化学习路由器 | 5 天 | P0-4 | - | `rl_router.py` + 单元测试 |
| P2-2 | Self-Correction Loop | 4 天 | P1-2 | - | `self_correction.py` + 单元测试 |
| **P2-3** | **ReAct 优化器** | **5 天** | **P1-2, P1-3** | **-** | **`react_optimizer.py` + 单元测试** |
| P2-4 | OPA 集成 (可选) | 3 天 | P1-5 | - | OPA 配置 + Rego 策略 |
| P2-5 | 监控告警 | 4 天 | 全部 | - | 监控面板 + 告警规则 |
| P2-6 | 性能优化 | 5 天 | 全部 | - | 性能报告 |

#### 里程碑

- [ ] M11: RL 路由器完成 (Day 47)
- [ ] M12: Self-Correction 完成 (Day 51)
- [ ] **M13: ReAct 优化器完成 (Day 56)**
- [ ] M14: 监控告警上线 (Day 59)
- [ ] M15: 项目终验 (Day 64)

#### 预期收益

- 路由策略自优化，成本再降 20%
- 输出准确率提升至 98%+
- 实现 System 1 + System 2 协作
- **ReAct 任务平均步数减少 30%**
- **工具调用准确率提升至 95%+**

---

## 风险与应对

### 技术风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 混合模型显存占用过高 | 中 | 高 | 使用量化版本 (INT4/INT8)、模型卸载 |
| 流式检测误报率高 | 中 | 中 | 调整阈值、增加白名单、人工审核通道 |
| RL 路由器收敛慢 | 高 | 中 | 预训练策略、模拟数据增强、降低学习率 |
| OPA 集成复杂度高 | 中 | 低 | 使用内置策略引擎降级方案 |

### 进度风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 关键人员请假 | 低 | 高 | 文档化、代码审查、知识共享 |
| 依赖库兼容性问题 | 中 | 中 | 提前验证、准备备选方案 |
| 测试发现重大 Bug | 中 | 高 | 预留缓冲时间、分阶段上线 |

### 业务风险

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 重构影响现有功能 | 中 | 高 | 完整回归测试、灰度发布、快速回滚 |
| 性能提升不达预期 | 低 | 中 | 建立基准、持续优化、调整阈值 |

---

## 验收标准

### 功能验收

| 功能 | 验收标准 | 测试方法 |
|------|----------|----------|
| WAF 规则引擎 | 拦截 SQL 注入/XSS/Prompt 注入 | 注入测试用例 |
| 脏数据过滤 | 识别并拒绝低质量输入 | 垃圾内容测试 |
| 动态路由 | 80% 简单请求路由到 FAST 通道 | 路由日志分析 |
| Flow 引擎 | 成功执行预定义工作流 | 工作流测试 |
| **ReAct 引擎** | **完成多步任务，轨迹完整可解释** | **ReAct 任务测试** |
| **工具注册表** | **支持 15+ 工具，准确率>95%** | **工具调用测试** |
| **轨迹记录** | **完整记录 Thought-Action-Observation** | **轨迹回放测试** |
| 混合模型 | 成功加载 Jamba/Falcon-H1 | 模型加载测试 |
| 流式检测 | 违规内容 50ms 内切断 | 延迟测试 |
| 语义审计 | 识别隐晦恶意意图 | 对抗样本测试 |

### 性能验收

| 指标 | 当前值 | 目标值 | 测试方法 |
|------|--------|--------|----------|
| 简单请求延迟 | 300ms | 100ms (阶段一) / 40ms (阶段三) | 压测 |
| 复杂推理准确率 | 85% | 92% (阶段二) / 96% (阶段三) | 样本测试 |
| 长文本上限 | 32K | 256K (阶段二) / 1M+ (阶段三) | 压力测试 |
| Token 成本 | 100% | 40% (阶段二) / 20% (阶段三) | 成本分析 |
| 安全拦截率 | 80% | 95% (阶段一) / 99% (阶段三) | 对抗测试 |
| **Agent 任务完成率** | **60%** | **80% (阶段二) / 90% (阶段三)** | **ReAct 任务测试** |
| **ReAct 平均步数** | **N/A** | **<5 步** | **轨迹分析** |
| **工具调用准确率** | **N/A** | **>95%** | **工具调用测试** |

### 代码质量验收

| 指标 | 目标值 | 测量方法 |
|------|--------|----------|
| 单元测试覆盖率 | >80% | pytest --cov |
| 代码审查通过率 | 100% | PR 审查 |
| 文档完整度 | >90% | 文档检查清单 |
| 技术债务 | 无新增 | SonarQube 扫描 |

---

## 附录

### A. 新增文件清单

```
server/
├── security/
│   ├── waf_engine.py              # P0-1: WAF 规则引擎
│   ├── gateway_middleware.py      # P0-5: 网关中间件
│   ├── streaming_detector.py      # P1-6: 流式检测器
│   ├── semantic_auditor.py        # P1-7: 语义审计器
│   └── policy_engine.py           # P1-8: 策略引擎
│
├── core/
│   ├── content_filter.py          # P0-2: 脏数据过滤器
│   ├── entropy_calculator.py      # P0-3: 熵值计算器
│   ├── dynamic_router.py          # P0-4: 动态路由器
│   ├── flow_engine.py             # P1-1: Flow 工作流引擎
│   ├── react_engine.py            # P1-2: ReAct 引擎 (谷歌架构)
│   ├── tool_registry.py           # P1-3: 工具注册表
│   ├── trajectory_logger.py       # P1-4: 推理轨迹记录器
│   ├── rl_router.py               # P2-1: 强化学习路由器
│   ├── self_correction.py         # P2-2: 自我修正循环
│   └── react_optimizer.py         # P2-3: ReAct 优化器
│
└── ai/
    └── hybrid_model_adapter.py    # P1-5: 混合模型适配器
```

### B. 配置变更

#### 新增环境变量 (.env)

```ini
# L1 网关配置
WAF_ENABLED=true
CONTENT_FILTER_ENABLED=true

# L2 路由配置
ROUTER_STRATEGY=dynamic  # dynamic / rl / manual
FAST_PATH_ENTROPY_THRESHOLD=3.5
SLOW_PATH_ENTROPY_THRESHOLD=4.0

# L3 混合模型配置
DEFAULT_HYBRID_MODEL=qwen-mamba-7b
ENABLE_FLOW_MODE=true

# L4 守卫配置
STREAMING_DETECTION_ENABLED=true
SEMANTIC_AUDIT_LEVEL=standard  # basic / standard / deep
```

#### 新增配置文件 (config/architecture.yaml)

```yaml
architecture:
  version: "2.0"
  layers:
    l1_gateway:
      enabled: true
      waf_rules: default
      content_filter: true
    
    l2_router:
      enabled: true
      strategy: dynamic
      entropy_thresholds:
        fast_max: 3.5
        slow_min: 4.0
      complexity_thresholds:
        fast_max: 0.3
        slow_min: 0.5
    
    l3_execution:
      modes:
        - chat
        - flow
        - react
      models:
        fast: mamba_small
        slow: transformer_large
        long: hybrid
    
    l4_guard:
      enabled: true
      streaming_detection: true
      semantic_audit: standard
      policy_engine: builtin
```

### C. API 变更

#### 新增 API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/gateway/route` | POST | 手动路由测试 |
| `/gateway/audit` | GET | 获取审计日志 |
| `/flows` | GET | 列出可用工作流 |
| `/flows/{id}/execute` | POST | 执行工作流 |
| `/models/hybrid/recommend` | GET | 获取模型推荐 |

#### 现有 API 增强

| 端点 | 变更 |
|------|------|
| `/chat` | 增加 `routing_info` 响应字段 |
| `/inference/generate` | 增加 `model_type` 参数 |

### D. 测试计划

#### 单元测试

```bash
# 阶段一
pytest tests/unit/security/test_waf_engine.py
pytest tests/unit/core/test_content_filter.py
pytest tests/unit/core/test_entropy_calculator.py
pytest tests/unit/core/test_dynamic_router.py
pytest tests/unit/security/test_gateway_middleware.py

# 阶段二
pytest tests/unit/core/test_flow_engine.py
pytest tests/unit/core/test_react_engine.py        # ReAct 引擎测试
pytest tests/unit/core/test_tool_registry.py      # 工具注册表测试
pytest tests/unit/core/test_trajectory_logger.py  # 轨迹记录测试
pytest tests/unit/ai/test_hybrid_model_adapter.py
pytest tests/unit/security/test_streaming_detector.py
pytest tests/unit/security/test_semantic_auditor.py
pytest tests/unit/security/test_policy_engine.py

# 阶段三
pytest tests/unit/core/test_rl_router.py
pytest tests/unit/core/test_self_correction.py
pytest tests/unit/core/test_react_optimizer.py    # ReAct 优化器测试
```

#### 集成测试

```bash
# 端到端测试
pytest tests/integration/test_layered_architecture.py
pytest tests/integration/test_routing_pipeline.py
pytest tests/integration/test_security_guards.py
pytest tests/integration/test_react_agent.py         # ReAct Agent 端到端测试
pytest tests/integration/test_tool_execution.py      # 工具执行测试

# 性能测试
pytest tests/performance/test_latency.py
pytest tests/performance/test_throughput.py
pytest tests/performance/test_cost.py
pytest tests/performance/test_react_efficiency.py    # ReAct 效率测试
```

### E. 监控指标

#### Prometheus 指标

```yaml
# L1 网关
gateway_requests_total{status="allowed|blocked"}
gateway_waf_violations_total{rule_id="*"}
gateway_content_filter_rejections_total

# L2 路由
routing_decisions_total{path="fast|slow|long|react|flow"}
routing_entropy_distribution{bucket="*"}
routing_latency_seconds

# L3 执行
execution_mode_invocations_total{mode="chat|flow|react"}
model_inference_latency_seconds{model_type="*"}
model_token_usage_total{model_type="*"}

# L3 ReAct
react_tasks_total{status="success|failure|timeout"}
react_steps_per_task{bucket="*"}
react_tool_calls_total{tool_name="*"}
react_tool_error_rate{tool_name="*"}
react_trajectory_length{bucket="*"}

# L4 守卫
guard_streaming_violations_total{type="*"}
guard_semantic_audit_results{passed="true|false"}
guard_policy_decisions_total{allowed="true|false"}
```

### F. 回滚计划

若重构后出现重大问题，按以下步骤回滚：

1. **立即回滚** (5 分钟内)：
   ```bash
   # 切换配置
   export ARCHITECTURE_VERSION=1.0
   # 重启服务
   systemctl restart finetune-platform
   ```

2. **代码回滚** (30 分钟内)：
   ```bash
   git checkout <重构前 commit>
   pip install -r requirements.txt
   systemctl restart finetune-platform
   ```

3. **数据回滚** (如有需要)：
   ```bash
   # 恢复数据库备份
   sqlite3 data/finetune.db ".restore data/backup.db"
   ```

---

## 审批签字

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 项目经理 | | | |
| 技术负责人 | | | |
| 产品负责人 | | | |

---

**文档结束**
