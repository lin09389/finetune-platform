# 🧠 智能记忆系统 - 实现计划

> 结合《长期记忆实现指南》和《智能记忆系统实现方案》，基于 Finetune Platform 现有架构设计

---

## 📊 项目现状分析

### ✅ 已有基础设施（可直接复用）

| 组件 | 文件 | 功能 | 复用方式 |
|------|------|------|----------|
| **嵌入模型** | `server/rag/embedder.py` | text2vec-base-chinese (768维) | 直接复用 |
| **向量存储** | `server/rag/vector_store.py` | ChromaDB | 直接复用 |
| **RAG 服务** | `server/rag/service.py` | 向量化、搜索 | 扩展复用 |
| **聊天历史** | `server/api/chat_history.py` | SQLite 会话管理 | 扩展复用 |
| **前端聊天** | `client/src/pages/Chat.tsx` | 聊天界面 | 集成记忆 |

### 🆕 需要新增的模块

```
server/
├── memory/                      # 新增：记忆模块
│   ├── __init__.py
│   ├── memory_service.py        # 记忆服务核心
│   ├── memory_extractor.py      # 记忆提取器
│   └── memory_manager.py        # 记忆管理器
├── api/
│   └── memory.py                # 新增：记忆 API
└── data/
    └── memories/                # 新增：记忆向量库

client/src/
├── components/
│   └── MemoryManager.tsx        # 新增：记忆管理组件
└── pages/
    └── Chat.tsx                 # 修改：集成记忆提取
```

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户对话                                 │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Chat.tsx (前端)                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │  发送消息   │──│  提取记忆   │──│  显示记忆提示          │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
                              │ API 调用
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Memory API (后端)                             │
│  POST /memory/extract    - 提取记忆                             │
│  POST /memory/recall     - 检索记忆                             │
│  GET  /memory/list       - 列出记忆                             │
│  DELETE /memory/{id}     - 删除记忆                             │
│  GET  /memory/summary    - 用户摘要                             │
└─────────────────────────────┬───────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                 MemoryService (核心服务)                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │  记忆提取器     │  │  记忆存储       │  │  记忆检索       │ │
│  │  (规则 + LLM)   │  │  (向量 + 元数据) │  │  (相似度搜索)   │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
└─────────────────────────────┬───────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Embedder       │  │  VectorStore    │  │  SQLite         │
│  (复用 RAG)     │  │  (复用 RAG)     │  │  (复用聊天历史) │
│  text2vec       │  │  ChromaDB       │  │  记忆元数据     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### 数据流

```
1. 记忆提取流程
   用户消息 → 规则提取 → LLM 提取 → 去重 → 向量化 → 存储

2. 记忆检索流程
   用户查询 → 向量化 → 相似度搜索 → 排序 → 返回 Top-K

3. 记忆注入流程
   用户查询 → 检索相关记忆 → 构建上下文 → 注入 System Prompt
```

---

## 📝 详细实现方案

### Phase 1: 后端核心模块（预计 4 小时）

#### 1.1 记忆数据模型

```python
# server/memory/models.py

from dataclasses import dataclass
from datetime import datetime
from typing import List, Optional
from enum import Enum

class MemoryType(str, Enum):
    """记忆类型"""
    PERSONAL = "personal"       # 个人信息（名字、职业、住址）
    PREFERENCE = "preference"   # 用户偏好（喜欢/讨厌）
    PROJECT = "project"         # 项目信息（在做什么）
    SKILL = "skill"             # 技能知识（会什么）
    HABIT = "habit"             # 工作习惯（编码风格）
    HISTORY = "history"         # 历史事件（过去发生的事）
    KNOWLEDGE = "knowledge"     # 知识积累（学到的知识）

@dataclass
class Memory:
    """记忆数据结构"""
    id: str
    content: str                    # 记忆内容
    memory_type: MemoryType         # 记忆类型
    importance: float               # 重要程度 (0-1)
    source: str                     # 来源（rule/llm/manual）
    created_at: datetime            # 创建时间
    last_accessed: datetime         # 最后访问时间
    access_count: int               # 访问次数
    embedding: Optional[List[float]] = None  # 向量嵌入

# 记忆类型重要性映射
MEMORY_IMPORTANCE = {
    MemoryType.PERSONAL: 0.9,       # 个人信息最重要
    MemoryType.PREFERENCE: 0.8,     # 偏好很重要
    MemoryType.PROJECT: 0.7,        # 项目信息重要
    MemoryType.SKILL: 0.6,          # 技能中等
    MemoryType.HABIT: 0.5,          # 习惯一般
    MemoryType.HISTORY: 0.4,        # 历史较不重要
    MemoryType.KNOWLEDGE: 0.3,      # 知识最不重要
}
```

#### 1.2 记忆提取器

```python
# server/memory/memory_extractor.py

import re
from typing import List, Dict, Any
from .models import Memory, MemoryType, MEMORY_IMPORTANCE

class MemoryExtractor:
    """记忆提取器"""

    def __init__(self):
        # 提取规则（正则模式）
        self.patterns = {
            MemoryType.PERSONAL: [
                (r'我叫\s*(\S+)', '用户的名字是 {1}'),
                (r'我是\s*(\S+?)(?:，|。|！|$)', '用户的身份是 {1}'),
                (r'我在?\s*(.+?)工作', '用户在 {1} 工作'),
                (r'我住[在是]\s*(.+?)[，。]', '用户住在 {1}'),
                (r'我的(\S+)是\s*(\S+)', '用户的{1}是{2}'),
                (r'我(\d+)岁', '用户{1}岁'),
            ],
            MemoryType.PREFERENCE: [
                (r'我喜欢用?\s*(\S+)', '用户喜欢用 {1}'),
                (r'我喜欢\s+(\S+)', '用户喜欢 {1}'),
                (r'我讨厌\s+(\S+)', '用户讨厌 {1}'),
                (r'我偏好\s+(\S+)', '用户偏好 {1}'),
                (r'我常用\s+(\S+)', '用户常用 {1}'),
                (r'我不用\s+(\S+)', '用户不用 {1}'),
            ],
            MemoryType.PROJECT: [
                (r'我在做?\s*(.+?)项目', '用户在做 {1} 项目'),
                (r'我的项目用?\s*(\S+)', '用户的项目使用 {1}'),
                (r'我在开发?\s*(.+)', '用户在开发 {1}'),
                (r'我在研究?\s*(.+)', '用户在研究 {1}'),
            ],
            MemoryType.SKILL: [
                (r'我会\s+(\S+)', '用户会 {1}'),
                (r'我精通?\s+(\S+)', '用户精通 {1}'),
                (r'我熟悉?\s+(\S+)', '用户熟悉 {1}'),
                (r'我正在学?\s*(\S+)', '用户正在学习 {1}'),
            ],
            MemoryType.HABIT: [
                (r'我习惯?\s+(.+)', '用户习惯 {1}'),
                (r'我一般\s+(.+)', '用户一般 {1}'),
                (r'我通常?\s+(.+)', '用户通常 {1}'),
            ],
        }

        # 重要关键词（触发记忆提取）
        self.important_keywords = [
            '记住', '别忘了', '记得', 'important',
            '我的', '我家', '我公司', '我学校', '我团队'
        ]

    def extract(self, message: str, role: str = 'user') -> List[Dict[str, Any]]:
        """
        从消息中提取记忆

        Args:
            message: 消息内容
            role: 角色（user/assistant）

        Returns:
            提取的记忆列表
        """
        if role != 'user':
            return []

        memories = []

        # 1. 规则提取
        rule_memories = self._rule_extraction(message)
        memories.extend(rule_memories)

        # 2. 关键词提取
        keyword_memories = self._keyword_extraction(message)
        memories.extend(keyword_memories)

        # 3. 去重
        memories = self._deduplicate(memories)

        return memories

    def _rule_extraction(self, message: str) -> List[Dict[str, Any]]:
        """规则提取"""
        memories = []

        for mem_type, patterns in self.patterns.items():
            for pattern, template in patterns:
                matches = re.finditer(pattern, message, re.IGNORECASE)
                for match in matches:
                    # 替换模板
                    content = template
                    for i, group in enumerate(match.groups(), 1):
                        content = content.replace(f'{{{i}}}', group or '')

                    memories.append({
                        'content': content,
                        'type': mem_type.value,
                        'importance': MEMORY_IMPORTANCE.get(mem_type, 0.5),
                        'source': 'rule',
                        'raw_text': match.group(0)
                    })

        return memories

    def _keyword_extraction(self, message: str) -> List[Dict[str, Any]]:
        """关键词提取"""
        memories = []

        # 检查是否包含重要关键词
        has_important = any(kw in message.lower() for kw in self.important_keywords)

        if has_important:
            # 提取包含关键词的句子
            sentences = re.split(r'[.。!！?？\n]', message)
            for sentence in sentences:
                sentence = sentence.strip()
                if sentence and len(sentence) > 10:
                    # 检查句子是否包含重要信息
                    if any(kw in sentence for kw in self.important_keywords):
                        memories.append({
                            'content': sentence,
                            'type': MemoryType.KNOWLEDGE.value,
                            'importance': 0.6,
                            'source': 'keyword'
                        })

        return memories

    def _deduplicate(self, memories: List[Dict]) -> List[Dict]:
        """去重"""
        seen = set()
        unique = []
        for mem in memories:
            key = mem['content']
            if key not in seen:
                seen.add(key)
                unique.append(mem)
        return unique
```

#### 1.3 记忆服务

```python
# server/memory/memory_service.py

from typing import List, Dict, Any, Optional
from datetime import datetime
import uuid
import logging

from rag.embedder import get_embedder
from rag.vector_store import get_vector_store
from .memory_extractor import MemoryExtractor
from .models import MemoryType, MEMORY_IMPORTANCE

logger = logging.getLogger(__name__)


class MemoryService:
    """智能记忆服务"""

    def __init__(self, vector_db_path: str = "data/memories"):
        # 复用 RAG 的嵌入模型
        self.embedder = get_embedder("shibing624/text2vec-base-chinese")

        # 复用 RAG 的向量存储（独立路径）
        self.vector_store = get_vector_store(vector_db_path)

        # 记忆提取器
        self.extractor = MemoryExtractor()

    def extract_and_store(
        self,
        message: str,
        role: str,
        user_id: str = "default"
    ) -> List[Dict[str, Any]]:
        """
        提取并存储记忆

        Args:
            message: 消息内容
            role: 角色
            user_id: 用户 ID

        Returns:
            存储的记忆列表
        """
        # 1. 提取记忆
        extracted = self.extractor.extract(message, role)

        if not extracted:
            return []

        # 2. 存储记忆
        stored = []
        for mem in extracted:
            try:
                memory_id = self._store_memory(user_id, mem)
                stored.append({
                    'id': memory_id,
                    'content': mem['content'],
                    'type': mem['type']
                })
                logger.info(f"记忆已存储: {mem['content'][:50]}...")
            except Exception as e:
                logger.error(f"存储记忆失败: {e}")

        return stored

    def _store_memory(self, user_id: str, memory: Dict) -> str:
        """存储单条记忆"""
        memory_id = f"mem_{uuid.uuid4().hex[:8]}"

        # 向量化
        embedding = self.embedder.embed_single(memory['content'])

        # 元数据
        metadata = {
            'user_id': user_id,
            'type': memory['type'],
            'importance': memory['importance'],
            'source': memory.get('source', 'unknown'),
            'created_at': datetime.now().isoformat(),
            'access_count': 0
        }

        # 存储到向量库
        self.vector_store.add_documents(
            collection_name=f"memories_{user_id}",
            documents=[memory['content']],
            embeddings=[embedding],
            metadatas=[metadata],
            ids=[memory_id]
        )

        return memory_id

    def recall(
        self,
        query: str,
        user_id: str = "default",
        top_k: int = 5,
        memory_type: Optional[str] = None
    ) -> List[Dict[str, Any]]:
        """
        检索相关记忆

        Args:
            query: 查询文本
            user_id: 用户 ID
            top_k: 返回数量
            memory_type: 记忆类型过滤

        Returns:
            相关记忆列表
        """
        # 向量化查询
        query_embedding = self.embedder.embed_single(query)

        # 构建过滤条件
        filter_metadata = None
        if memory_type:
            filter_metadata = {"type": memory_type}

        # 搜索
        results = self.vector_store.search(
            collection_name=f"memories_{user_id}",
            query_embedding=query_embedding,
            top_k=top_k,
            filter_metadata=filter_metadata
        )

        # 格式化结果
        memories = []
        for result in results:
            memories.append({
                'id': result.get('metadata', {}).get('id', ''),
                'content': result['content'],
                'type': result.get('metadata', {}).get('type', 'general'),
                'importance': result.get('metadata', {}).get('importance', 0.5),
                'created_at': result.get('metadata', {}).get('created_at', ''),
                'access_count': result.get('metadata', {}).get('access_count', 0),
                'relevance': result.get('score', 0)
            })

        return memories

    def list_memories(
        self,
        user_id: str = "default",
        memory_type: Optional[str] = None,
        limit: int = 50
    ) -> List[Dict[str, Any]]:
        """
        列出所有记忆

        Args:
            user_id: 用户 ID
            memory_type: 记忆类型过滤
            limit: 返回数量

        Returns:
            记忆列表
        """
        collection = self.vector_store.get_or_create_collection(
            f"memories_{user_id}"
        )

        all_data = collection.get(include=["metadatas", "documents"])

        memories = []
        if all_data['metadatas'] and all_data['documents']:
            for i, (meta, doc) in enumerate(
                zip(all_data['metadatas'], all_data['documents'])
            ):
                if memory_type and meta.get('type') != memory_type:
                    continue

                memories.append({
                    'id': all_data['ids'][i],
                    'content': doc,
                    'type': meta.get('type', 'general'),
                    'importance': meta.get('importance', 0.5),
                    'created_at': meta.get('created_at', ''),
                    'access_count': meta.get('access_count', 0)
                })

        # 按重要性排序
        memories.sort(key=lambda x: x['importance'], reverse=True)

        return memories[:limit]

    def forget(self, user_id: str, memory_id: str) -> bool:
        """
        删除记忆

        Args:
            user_id: 用户 ID
            memory_id: 记忆 ID

        Returns:
            是否成功
        """
        try:
            self.vector_store.delete_documents(
                collection_name=f"memories_{user_id}",
                ids=[memory_id]
            )
            logger.info(f"记忆已删除: {memory_id}")
            return True
        except Exception as e:
            logger.error(f"删除记忆失败: {e}")
            return False

    def get_context_with_memory(
        self,
        query: str,
        user_id: str = "default",
        max_memories: int = 5
    ) -> str:
        """
        获取包含记忆的上下文

        Args:
            query: 查询文本
            user_id: 用户 ID
            max_memories: 最大记忆数量

        Returns:
            格式化的上下文
        """
        # 检索相关记忆
        memories = self.recall(query, user_id, top_k=max_memories)

        if not memories:
            return ""

        # 构建上下文
        context_parts = ["【相关记忆】"]
        for mem in memories:
            context_parts.append(f"- {mem['content']}")

        return "\n".join(context_parts)

    def get_user_summary(self, user_id: str = "default") -> Dict[str, Any]:
        """
        获取用户记忆摘要

        Args:
            user_id: 用户 ID

        Returns:
            摘要信息
        """
        memories = self.list_memories(user_id, limit=100)

        # 按类型分组
        by_type = {}
        for mem in memories:
            mem_type = mem['type']
            if mem_type not in by_type:
                by_type[mem_type] = []
            by_type[mem_type].append(mem['content'])

        # 统计
        type_labels = {
            'personal': '个人信息',
            'preference': '偏好',
            'project': '项目',
            'skill': '技能',
            'habit': '习惯',
            'history': '历史',
            'knowledge': '知识'
        }

        summary = {
            'total_count': len(memories),
            'by_type': {
                type_labels.get(k, k): v
                for k, v in by_type.items()
            }
        }

        return summary


# 单例
_memory_service = None

def get_memory_service() -> MemoryService:
    """获取记忆服务实例"""
    global _memory_service
    if _memory_service is None:
        _memory_service = MemoryService()
    return _memory_service
```

#### 1.4 记忆 API

```python
# server/api/memory.py

from fastapi import APIRouter, HTTPException, Query
from pydantic import BaseModel, Field
from typing import List, Optional, Dict, Any

from memory.memory_service import get_memory_service

router = APIRouter()


# ============ 请求/响应模型 ============

class ExtractRequest(BaseModel):
    """提取请求"""
    message: str = Field(..., description="消息内容")
    role: str = Field(default="user", description="角色")
    user_id: str = Field(default="default", description="用户 ID")


class RecallRequest(BaseModel):
    """回忆请求"""
    query: str = Field(..., description="查询文本")
    user_id: str = Field(default="default", description="用户 ID")
    top_k: int = Field(default=5, description="返回数量")
    memory_type: Optional[str] = Field(default=None, description="记忆类型")


class MemoryResponse(BaseModel):
    """记忆响应"""
    id: str
    content: str
    type: str
    importance: float
    created_at: str
    access_count: int = 0
    relevance: Optional[float] = None


# ============ API 端点 ============

@router.post("/extract")
async def extract_memory(request: ExtractRequest):
    """
    从消息中提取记忆

    自动识别用户消息中的重要信息并存储
    """
    try:
        service = get_memory_service()
        memories = service.extract_and_store(
            message=request.message,
            role=request.role,
            user_id=request.user_id
        )

        return {
            "success": True,
            "extracted": len(memories),
            "memories": memories
        }
    except Exception as e:
        raise HTTPException(500, f"提取失败: {str(e)}")


@router.post("/recall")
async def recall_memory(request: RecallRequest):
    """
    检索相关记忆

    根据查询文本返回相关的记忆
    """
    try:
        service = get_memory_service()
        memories = service.recall(
            query=request.query,
            user_id=request.user_id,
            top_k=request.top_k,
            memory_type=request.memory_type
        )

        return {
            "success": True,
            "memories": memories,
            "count": len(memories)
        }
    except Exception as e:
        raise HTTPException(500, f"检索失败: {str(e)}")


@router.get("/list")
async def list_memories(
    user_id: str = Query(default="default"),
    memory_type: Optional[str] = Query(default=None),
    limit: int = Query(default=50)
):
    """
    列出所有记忆

    支持按类型过滤
    """
    try:
        service = get_memory_service()
        memories = service.list_memories(
            user_id=user_id,
            memory_type=memory_type,
            limit=limit
        )

        return {
            "success": True,
            "count": len(memories),
            "memories": memories
        }
    except Exception as e:
        raise HTTPException(500, f"获取失败: {str(e)}")


@router.delete("/{memory_id}")
async def forget_memory(
    memory_id: str,
    user_id: str = Query(default="default")
):
    """
    删除记忆

    用户主动遗忘某条记忆
    """
    try:
        service = get_memory_service()
        success = service.forget(user_id, memory_id)

        if success:
            return {"success": True, "message": "记忆已删除"}
        else:
            raise HTTPException(404, "记忆不存在")
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(500, f"删除失败: {str(e)}")


@router.get("/summary")
async def get_summary(user_id: str = Query(default="default")):
    """
    获取用户记忆摘要

    返回记忆统计和分类信息
    """
    try:
        service = get_memory_service()
        summary = service.get_user_summary(user_id)

        return {
            "success": True,
            "summary": summary
        }
    except Exception as e:
        raise HTTPException(500, f"获取摘要失败: {str(e)}")


@router.get("/context")
async def get_context(
    query: str = Query(..., description="查询文本"),
    user_id: str = Query(default="default"),
    max_memories: int = Query(default=5)
):
    """
    获取带记忆的上下文

    用于注入到聊天提示中
    """
    try:
        service = get_memory_service()
        context = service.get_context_with_memory(
            query=query,
            user_id=user_id,
            max_memories=max_memories
        )

        return {
            "success": True,
            "context": context
        }
    except Exception as e:
        raise HTTPException(500, f"获取上下文失败: {str(e)}")
```

---

### Phase 2: 前端集成（预计 3 小时）

#### 2.1 记忆管理组件

```tsx
// client/src/components/MemoryManager.tsx

import { useState, useEffect } from 'react'
import {
  Modal, List, Button, Tag, Empty, Spin, message,
  Tabs, Typography, Popconfirm, Input, Space
} from 'antd'
import {
  DeleteOutlined, ClockCircleOutlined,
  UserOutlined, HeartOutlined, ProjectOutlined,
  ToolOutlined, HistoryOutlined, BookOutlined,
  ThunderboltOutlined
} from '@ant-design/icons'

const { Search } = Input
const { Text } = Typography

interface Memory {
  id: string
  content: string
  type: string
  importance: number
  created_at: string
  access_count: number
  relevance?: number
}

interface MemoryManagerProps {
  open: boolean
  onClose: () => void
}

const typeConfig: Record<string, { color: string; label: string; icon: any }> = {
  personal: { color: 'green', label: '个人信息', icon: <UserOutlined /> },
  preference: { color: 'blue', label: '偏好', icon: <HeartOutlined /> },
  project: { color: 'purple', label: '项目', icon: <ProjectOutlined /> },
  skill: { color: 'orange', label: '技能', icon: <ToolOutlined /> },
  habit: { color: 'cyan', label: '习惯', icon: <ThunderboltOutlined /> },
  history: { color: 'gray', label: '历史', icon: <HistoryOutlined /> },
  knowledge: { color: 'gold', label: '知识', icon: <BookOutlined /> },
}

export default function MemoryManager({ open, onClose }: MemoryManagerProps) {
  const [memories, setMemories] = useState<Memory[]>([])
  const [loading, setLoading] = useState(false)
  const [activeType, setActiveType] = useState<string>('all')
  const [searchQuery, setSearchQuery] = useState('')

  useEffect(() => {
    if (open) {
      loadMemories()
    }
  }, [open, activeType])

  const loadMemories = async () => {
    setLoading(true)
    try {
      const url = activeType === 'all'
        ? 'http://127.0.0.1:8000/memory/list'
        : `http://127.0.0.1:8000/memory/list?memory_type=${activeType}`

      const response = await fetch(url)
      const data = await response.json()
      setMemories(data.memories || [])
    } catch (error) {
      message.error('加载记忆失败')
    } finally {
      setLoading(false)
    }
  }

  const searchMemories = async () => {
    if (!searchQuery.trim()) {
      loadMemories()
      return
    }

    setLoading(true)
    try {
      const response = await fetch('http://127.0.0.1:8000/memory/recall', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ query: searchQuery, top_k: 20 })
      })
      const data = await response.json()
      setMemories(data.memories || [])
    } catch (error) {
      message.error('搜索失败')
    } finally {
      setLoading(false)
    }
  }

  const deleteMemory = async (memoryId: string) => {
    try {
      await fetch(`http://127.0.0.1:8000/memory/${memoryId}`, {
        method: 'DELETE'
      })
      message.success('已删除')
      loadMemories()
    } catch (error) {
      message.error('删除失败')
    }
  }

  const filteredMemories = memories

  return (
    <Modal
      title="🧠 记忆管理"
      open={open}
      onCancel={onClose}
      footer={null}
      width={700}
    >
      {/* 搜索栏 */}
      <Space style={{ width: '100%', marginBottom: 16 }}>
        <Search
          placeholder="搜索记忆..."
          value={searchQuery}
          onChange={e => setSearchQuery(e.target.value)}
          onSearch={searchMemories}
          style={{ width: 300 }}
          enterButton
        />
        <Text type="secondary">共 {memories.length} 条记忆</Text>
      </Space>

      {/* 类型过滤 */}
      <Tabs
        activeKey={activeType}
        onChange={setActiveType}
        items={[
          { key: 'all', label: '全部' },
          ...Object.entries(typeConfig).map(([key, config]) => ({
            key,
            label: (
              <span>
                {config.icon} {config.label}
              </span>
            )
          }))
        ]}
      />

      {/* 记忆列表 */}
      {loading ? (
        <Spin style={{ display: 'block', margin: '40px auto' }} />
      ) : filteredMemories.length === 0 ? (
        <Empty description="暂无记忆" />
      ) : (
        <List
          dataSource={filteredMemories}
          renderItem={(memory) => {
            const config = typeConfig[memory.type] || {
              color: 'default',
              label: memory.type,
              icon: null
            }

            return (
              <List.Item
                actions={[
                  <Popconfirm
                    key="delete"
                    title="确定删除这条记忆？"
                    onConfirm={() => deleteMemory(memory.id)}
                  >
                    <Button type="text" danger icon={<DeleteOutlined />} />
                  </Popconfirm>
                ]}
              >
                <List.Item.Meta
                  avatar={
                    <Tag color={config.color} style={{ padding: '4px 8px' }}>
                      {config.icon}
                    </Tag>
                  }
                  title={<Text>{memory.content}</Text>}
                  description={
                    <Space split={<span>|</span>}>
                      <span>
                        <ClockCircleOutlined /> {memory.created_at?.split('T')[0]}
                      </span>
                      <span>
                        重要度: {'⭐'.repeat(Math.round(memory.importance * 5))}
                      </span>
                      {memory.relevance && (
                        <span>相关度: {(memory.relevance * 100).toFixed(0)}%</span>
                      )}
                    </Space>
                  }
                />
              </List.Item>
            )
          }}
        />
      )}
    </Modal>
  )
}
```

#### 2.2 集成到 Chat.tsx

```tsx
// client/src/pages/Chat.tsx 修改部分

// 1. 导入记忆管理组件
import MemoryManager from '../components/MemoryManager'

// 2. 添加状态
const [memoryManagerOpen, setMemoryManagerOpen] = useState(false)
const [memoryHint, setMemoryHint] = useState<string[]>([])

// 3. 修改 handleSend 函数
const handleSend = async () => {
  if (!selectedModel || !inputValue.trim()) return

  const userMessage: Message = {
    id: `msg_${Date.now()}`,
    role: 'user',
    content: inputValue.trim(),
    timestamp: new Date().toISOString(),
  }

  setMessages(prev => [...prev, userMessage])
  setInputValue('')
  setLoading(true)

  // 🧠 新增：提取记忆（异步，不阻塞聊天）
  extractMemory(inputValue.trim())

  // ... 原有聊天逻辑
}

// 4. 添加记忆提取函数
const extractMemory = async (message: string) => {
  try {
    const response = await fetch('http://127.0.0.1:8000/memory/extract', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message, role: 'user' })
    })
    const data = await response.json()

    if (data.extracted > 0) {
      // 显示记忆提示
      setMemoryHint(data.memories.map((m: any) => m.content))
      setTimeout(() => setMemoryHint([]), 5000)
    }
  } catch (error) {
    // 提取失败不影响聊天
    console.warn('记忆提取失败:', error)
  }
}

// 5. 在 UI 中添加记忆按钮和提示
// 在工具栏添加：
<Button
  icon={<BulbOutlined />}
  onClick={() => setMemoryManagerOpen(true)}
>
  记忆
</Button>

// 在消息列表上方添加记忆提示：
{memoryHint.length > 0 && (
  <Alert
    type="info"
    message="💡 AI 记住了这些信息"
    description={
      <ul style={{ margin: 0, paddingLeft: 20 }}>
        {memoryHint.map((h, i) => <li key={i}>{h}</li>)}
      </ul>
    }
    closable
    onClose={() => setMemoryHint([])}
    style={{ marginBottom: 16 }}
  />
)}

// 6. 添加记忆管理弹窗
<MemoryManager
  open={memoryManagerOpen}
  onClose={() => setMemoryManagerOpen(false)}
/>
```

---

### Phase 3: 聊天集成记忆检索（预计 2 小时）

#### 3.1 修改推理 API

```python
# server/api/inference.py 修改部分

from memory.memory_service import get_memory_service

@router.post("/stream")
async def stream_inference(request: InferenceRequest):
    """流式推理（带记忆）"""

    # 1. 获取相关记忆
    memory_service = get_memory_service()
    memory_context = memory_service.get_context_with_memory(
        query=request.prompt,
        user_id=request.user_id or "default",
        max_memories=5
    )

    # 2. 构建带记忆的提示
    if memory_context:
        enhanced_prompt = f"""{memory_context}

用户问题：{request.prompt}

请根据以上记忆和上下文回答用户问题。如果记忆中有关于用户的信息，请自然地使用这些信息。"""
    else:
        enhanced_prompt = request.prompt

    # 3. 原有推理逻辑...
    # ...
```

---

## 📋 实现清单

### P0 - 核心功能（必须）

| 任务 | 文件 | 预计时间 | 状态 |
|------|------|----------|------|
| 记忆数据模型 | `server/memory/models.py` | 0.5h | ⬜ |
| 记忆提取器 | `server/memory/memory_extractor.py` | 1h | ⬜ |
| 记忆服务 | `server/memory/memory_service.py` | 1.5h | ⬜ |
| 记忆 API | `server/api/memory.py` | 1h | ⬜ |
| 注册路由 | `server/main.py` | 0.1h | ⬜ |

### P1 - 前端集成（重要）

| 任务 | 文件 | 预计时间 | 状态 |
|------|------|----------|------|
| 记忆管理组件 | `client/src/components/MemoryManager.tsx` | 2h | ⬜ |
| 集成到 Chat | `client/src/pages/Chat.tsx` | 1h | ⬜ |

### P2 - 聊天集成（增强）

| 任务 | 文件 | 预计时间 | 状态 |
|------|------|----------|------|
| 推理时注入记忆 | `server/api/inference.py` | 1h | ⬜ |
| 记忆提示 UI | `client/src/components/MemoryHint.tsx` | 0.5h | ⬜ |

### P3 - 优化（可选）

| 任务 | 描述 | 预计时间 |
|------|------|----------|
| LLM 智能提取 | 使用本地模型智能提取记忆 | 2h |
| 记忆过期清理 | 自动清理过期/不重要记忆 | 1h |
| 记忆导入导出 | JSON 格式导入导出 | 1h |
| 记忆重要性调整 | 用户手动调整重要性 | 0.5h |

---

## 🚀 实施计划

### Day 1（4 小时）- 后端核心

```
✅ 创建 memory 模块目录结构
✅ 实现 models.py（数据模型）
✅ 实现 memory_extractor.py（提取器）
✅ 实现 memory_service.py（服务层）
✅ 实现 memory.py（API 路由）
✅ 注册路由到 main.py
✅ 测试 API 端点
```

### Day 2（3 小时）- 前端集成

```
✅ 创建 MemoryManager.tsx 组件
✅ 集成到 Chat.tsx
✅ 添加记忆提取调用
✅ 添加记忆提示 UI
✅ 测试完整流程
```

### Day 3（2 小时）- 聊天集成

```
✅ 修改推理 API 注入记忆
✅ 测试记忆检索效果
✅ 优化提示词
✅ 完整测试
```

---

## 💡 技术决策

### 为什么选择 ChromaDB 而不是 LanceDB？

| 对比项 | ChromaDB | LanceDB |
|--------|----------|---------|
| **项目现状** | ✅ 已集成 | ❌ 需新增 |
| **开发成本** | 低（复用） | 高（新增） |
| **功能** | 满足需求 | 更强 |
| **依赖** | 已有 | 需新增 |

**结论**：复用现有 ChromaDB，降低开发成本

### 为什么选择规则提取 + 关键词提取？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **规则提取** | 快速、准确、可控 | 覆盖有限 |
| **LLM 提取** | 智能、全面 | 慢、依赖模型 |
| **关键词提取** | 简单、补充 | 精度一般 |

**结论**：P0 使用规则+关键词，P3 可添加 LLM 提取

---

## 📊 预期效果

### 用户体验

```
1. 用户说："我叫小明，喜欢用 Python"
   → AI 自动记住：用户的名字是小明，用户喜欢用 Python

2. 一周后用户问："我适合做什么项目？"
   → AI 检索记忆 → 回答："小明，根据你喜欢 Python，建议..."

3. 用户可以随时查看/删除记忆
   → 透明可控，隐私安全
```

### 差异化优势

```
vs ChatGPT：本地存储，隐私安全
vs Open WebUI：有记忆功能
vs Chatbox：有记忆功能

你的优势：
✅ 本地存储（隐私）
✅ 自动学习（智能）
✅ 可管理（透明）
✅ 复用现有架构（高效）
```

---

## 📝 备注

- 本计划基于项目现有架构设计，最大化复用已有组件
- 总开发时间约 9 小时（P0-P2）
- P3 优化项可根据需求逐步添加
- 建议先完成 P0-P1，验证效果后再进行 P2-P3

---

**准备好开始实现了吗？** 🚀