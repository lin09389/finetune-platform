# 🔒 安全机制实施计划 - 结合项目现状

**制定时间**: 2026-03-08  
**项目阶段**: 云端 AI 已集成，需要加强安全保护  
**预计工时**: 6-8 小时

---

## 📊 项目安全现状分析

### ✅ 已有安全功能

| 功能 | 位置 | 状态 | 说明 |
|------|------|------|------|
| Agent 安全机制 | `agent/config.py` | ✅ 完整 | 应用白名单/路径检查/文件扩展名限制 |
| API 速率限制 | `main.py` | ✅ 基础 | 每分钟请求数限制 |
| CORS 配置 | `main.py` | ✅ 完整 | 限制本地来源 |
| 审计日志 | `agent/audit.py` | ✅ 基础 | Agent 操作日志 |

### ⚠️ 需要加强的

| 问题 | 风险等级 | 影响范围 |
|------|----------|----------|
| API Key 明文存储 | 🔴 高 | 云端 AI 配置 |
| 文件沙箱不完整 | 🟡 中 | Agent 文件操作 |
| 审计日志分散 | 🟡 中 | 安全事件追踪 |
| 输入验证不足 | 🟡 中 | API 接口 |

### 🔍 代码审查发现

**API Key 存储问题**：
```python
# client/src/pages/Chat.tsx (L55)
localStorage.setItem('cloud_ai_config', JSON.stringify(config))
# ❌ 明文存储在浏览器本地存储

# ai/gateway.py (L109)
"Authorization": f"Bearer {api_key}"
# ⚠️ API Key 在服务器内存中明文传递
```

**文件操作问题**：
```python
# agent/executor.py
# ✅ 已有路径检查
# ⚠️ 但缺少文件大小限制、危险文件检测
```

---

## 🎯 实施目标

### P0 - 立即实施（本周）

| 功能 | 工作量 | 优先级 | 理由 |
|------|--------|--------|------|
| API Key 加密存储 | 2h | 🔴 | 云端 AI 已上线，Key 需要保护 |
| 前端配置加密 | 1h | 🔴 | 浏览器端也要加密 |

### P1 - 短期实施（下周）

| 功能 | 工作量 | 优先级 | 理由 |
|------|--------|--------|------|
| 文件沙箱增强 | 2h | 🟡 | Agent 功能需要加强 |
| 统一审计日志 | 2h | 🟡 | 便于问题追踪 |

### P2 - 中期实施（下月）

| 功能 | 工作量 | 优先级 | 理由 |
|------|--------|--------|------|
| 输入验证框架 | 1h | 🟢 | 提升整体安全性 |
| 安全中间件 | 2h | 🟢 | 速率限制/IP 黑名单 |

---

## 📝 详细实施计划

### 第 1 步：API Key 加密存储（2 小时）

**文件**: `server/security/encryption.py`

```python
"""
安全加密模块 - 加密存储敏感数据
"""
from cryptography.fernet import Fernet
from pathlib import Path
import base64
import os
import json

class SecureStorage:
    """安全存储 - 加密敏感数据"""

    def __init__(self):
        self.key_file = Path(__file__).parent.parent / "data" / ".encryption_key"
        self.vault_file = Path(__file__).parent.parent / "data" / ".vault"
        
        # 确保目录存在
        self.key_file.parent.mkdir(parents=True, exist_ok=True)
        
        # 获取或创建密钥
        self.key = self._get_or_create_key()
        self.cipher = Fernet(self.key)

    def _get_or_create_key(self) -> bytes:
        """获取或创建加密密钥"""
        if self.key_file.exists():
            with open(self.key_file, 'rb') as f:
                return f.read()
        else:
            # 生成新密钥
            key = Fernet.generate_key()
            with open(self.key_file, 'wb') as f:
                f.write(key)
            # 设置安全权限（仅所有者可读写）
            os.chmod(self.key_file, 0o600)
            return key

    def encrypt(self, plaintext: str) -> str:
        """加密字符串"""
        encrypted = self.cipher.encrypt(plaintext.encode())
        return base64.urlsafe_b64encode(encrypted).decode()

    def decrypt(self, ciphertext: str) -> str:
        """解密字符串"""
        encrypted = base64.urlsafe_b64decode(ciphertext.encode())
        decrypted = self.cipher.decrypt(encrypted)
        return decrypted.decode()

    def store_api_key(self, key_id: str, api_key: str):
        """存储 API Key"""
        vault = self._load_vault()
        vault[f"api_key:{key_id}"] = self.encrypt(api_key)
        self._save_vault(vault)

    def get_api_key(self, key_id: str) -> str:
        """获取 API Key"""
        vault = self._load_vault()
        encrypted = vault.get(f"api_key:{key_id}")
        if not encrypted:
            raise KeyError(f"API Key 不存在：{key_id}")
        return self.decrypt(encrypted)

    def delete_api_key(self, key_id: str):
        """删除 API Key"""
        vault = self._load_vault()
        vault.pop(f"api_key:{key_id}", None)
        self._save_vault(vault)

    def _load_vault(self) -> dict:
        """加载保险库"""
        if self.vault_file.exists():
            with open(self.vault_file, 'r', encoding='utf-8') as f:
                return json.load(f)
        return {}

    def _save_vault(self, vault: dict):
        """保存保险库"""
        with open(self.vault_file, 'w', encoding='utf-8') as f:
            json.dump(vault, f, indent=2, ensure_ascii=False)
        os.chmod(self.vault_file, 0o600)


# 全局单例
secure_storage = SecureStorage()
```

**修改**: `server/api/cloud_chat.py`

添加 API Key 管理功能，使用加密存储。

---

### 第 2 步：前端配置加密（1 小时）

**问题**：当前 API Key 明文存储在 localStorage

**方案 A**（简单）：Base64 编码
```javascript
// 存储
const encrypted = btoa(JSON.stringify(config))
localStorage.setItem('cloud_ai_config', encrypted)

// 读取
const decrypted = JSON.parse(atob(encrypted))
```

**方案 B**（推荐）：使用 crypto-js 库
```bash
npm install crypto-js
```

```typescript
import CryptoJS from 'crypto-js'

const SECRET_KEY = 'your-secret-key' // 应该从环境变量获取

// 加密
const encrypted = CryptoJS.AES.encrypt(
  JSON.stringify(config),
  SECRET_KEY
).toString()

// 解密
const decrypted = CryptoJS.AES.decrypt(
  encrypted,
  SECRET_KEY
).toString(CryptoJS.enc.Utf8)
```

---

### 第 3 步：文件沙箱增强（2 小时）

**文件**: `server/security/file_sandbox.py`

```python
"""
文件操作沙箱 - 限制文件访问范围
"""
from pathlib import Path
import os
import re
from typing import Set, List

class FileSandbox:
    """文件操作沙箱"""

    # 禁止访问的路径模式
    FORBIDDEN_PATTERNS = [
        r'\.\.',  # 目录遍历
        r'^/etc/',
        r'^/usr/',
        r'^C:/Windows/',
        r'^C:/Program Files/',
    ]

    # 允许的操作
    ALLOWED_OPERATIONS = {'read', 'write', 'create', 'delete', 'list'}

    # 允许的文件扩展名（创建/写入）
    ALLOWED_EXTENSIONS = {
        '.py', '.js', '.ts', '.jsx', '.tsx',
        '.json', '.yaml', '.yml',
        '.md', '.txt',
        '.csv', '.xml',
    }

    # 危险扩展名
    DANGEROUS_EXTENSIONS = {
        '.exe', '.bat', '.cmd', '.sh',
        '.dll', '.so', '.dylib',
        '.ps1', '.vbs',
    }

    def __init__(self, working_dir: str):
        self.working_dir = Path(working_dir).resolve()

    def safe_path(self, file_path: str, operation: str = 'read') -> Path:
        """安全检查文件路径"""
        if operation not in self.ALLOWED_OPERATIONS:
            raise PermissionError(f"不允许的操作：{operation}")

        # 转换为绝对路径
        if os.path.isabs(file_path):
            path = Path(file_path).resolve()
        else:
            path = (self.working_dir / file_path).resolve()

        # 检查是否在工作目录内
        try:
            path.relative_to(self.working_dir)
        except ValueError:
            raise PermissionError(f"禁止访问工作目录外的文件：{file_path}")

        # 检查禁止模式
        path_str = str(path).replace('\\', '/')
        for pattern in self.FORBIDDEN_PATTERNS:
            if re.search(pattern, path_str, re.IGNORECASE):
                raise PermissionError(f"禁止访问的路径：{file_path}")

        # 检查危险文件
        if operation in ['write', 'create']:
            ext = path.suffix.lower()
            if ext in self.DANGEROUS_EXTENSIONS:
                raise PermissionError(f"禁止创建危险文件类型：{ext}")

        return path

    def read_file(self, file_path: str, max_size: int = 10*1024*1024) -> str:
        """安全读取文件"""
        path = self.safe_path(file_path, 'read')
        
        # 检查文件大小
        if path.exists():
            file_size = path.stat().st_size
            if file_size > max_size:
                raise PermissionError(f"文件过大：{file_size/1024/1024:.2f}MB")

        with open(path, 'r', encoding='utf-8', errors='ignore') as f:
            return f.read()

    def write_file(self, file_path: str, content: str) -> str:
        """安全写入文件"""
        path = self.safe_path(file_path, 'write')
        
        # 检查扩展名
        ext = path.suffix.lower()
        if ext not in self.ALLOWED_EXTENSIONS:
            raise PermissionError(f"不允许的文件类型：{ext}")

        with open(path, 'w', encoding='utf-8') as f:
            f.write(content)

        return str(path)

    def list_files(self, directory: str = ".", pattern: str = "*") -> list:
        """安全列出文件"""
        dir_path = self.safe_path(directory, 'list')
        
        if not dir_path.is_dir():
            raise ValueError(f"不是目录：{directory}")

        files = []
        for item in dir_path.glob(pattern):
            if item.name.startswith('.'):
                continue
            
            files.append({
                'name': item.name,
                'path': str(item.relative_to(self.working_dir)),
                'is_dir': item.is_dir(),
                'size': item.stat().st_size if item.is_file() else 0
            })

        return files


# 全局单例
file_sandbox = FileSandbox(str(Path(__file__).parent.parent))
```

**修改**: `agent/executor.py`

集成 FileSandbox，替换现有文件操作逻辑。

---

### 第 4 步：统一审计日志（2 小时）

**文件**: `server/security/audit_log.py`

```python
"""
审计日志 - 记录所有敏感操作
"""
import logging
import json
from datetime import datetime
from pathlib import Path
from typing import Dict, Any

class AuditLogger:
    """审计日志器"""

    def __init__(self):
        self.log_dir = Path(__file__).parent.parent / "logs" / "audit"
        self.log_dir.mkdir(parents=True, exist_ok=True)

        # 配置日志
        self.logger = logging.getLogger('audit')
        self.logger.setLevel(logging.INFO)

        # 文件处理器
        log_file = self.log_dir / f"audit_{datetime.now().strftime('%Y%m%d')}.log"
        handler = logging.FileHandler(log_file, encoding='utf-8')
        handler.setFormatter(logging.Formatter('%(asctime)s - %(message)s'))
        self.logger.addHandler(handler)

        # 设置权限
        try:
            os.chmod(log_file, 0o600)
        except:
            pass

    def log_action(
        self,
        action: str,
        user_id: str = "anonymous",
        resource: str = None,
        details: Dict[str, Any] = None,
        success: bool = True
    ):
        """记录操作日志"""
        log_entry = {
            'timestamp': datetime.now().isoformat(),
            'action': action,
            'user_id': user_id,
            'resource': resource,
            'details': details or {},
            'success': success
        }

        self.logger.info(json.dumps(log_entry, ensure_ascii=False))

    def log_api_key_access(self, key_id: str, provider: str, success: bool):
        """记录 API Key 使用"""
        self.log_action(
            action='api_key_access',
            resource=key_id,
            details={'provider': provider},
            success=success
        )

    def log_file_operation(
        self,
        operation: str,
        file_path: str,
        success: bool = True
    ):
        """记录文件操作"""
        self.log_action(
            action=f'file_{operation}',
            resource=file_path,
            success=success
        )

    def log_security_event(
        self,
        event_type: str,
        details: Dict[str, Any],
        severity: str = 'medium'
    ):
        """记录安全事件"""
        self.log_action(
            action=f'security_{event_type}',
            details={**details, 'severity': severity},
            success=(severity != 'critical')
        )


# 全局单例
audit_logger = AuditLogger()
```

**集成点**：
1. `api/cloud_chat.py` - 记录 API Key 使用
2. `agent/executor.py` - 记录文件操作
3. `ai/gateway.py` - 记录云端 AI 调用

---

### 第 5 步：集成到主应用（1 小时）

**修改**: `server/main.py`

```python
# 导入安全模块
from security.encryption import secure_storage
from security.audit_log import audit_logger
from security.file_sandbox import file_sandbox

# 在 lifespan 中初始化
@asynccontextmanager
async def lifespan(app: FastAPI):
    # 启动时
    audit_logger.log_action(
        action='server_start',
        details={'version': '2.0.0'}
    )
    
    yield
    
    # 关闭时
    audit_logger.log_action(
        action='server_shutdown',
        details={'reason': 'normal'}
    )

# 注册路由（添加安全端点）
@app.get("/security/audit-logs")
async def get_audit_logs(date: str = None, limit: int = 100):
    """获取审计日志"""
    logs = audit_logger.get_logs(date=date, limit=limit)
    return {"logs": logs}
```

---

## 🗓️ 时间安排

### 方案 A：快速实施（推荐）

**总时间**: 6 小时

| 步骤 | 时间 | 产出 |
|------|------|------|
| 1. API Key 加密 | 2h | encryption.py + cloud_chat 修改 |
| 2. 前端加密 | 1h | 浏览器端加密 |
| 3. 文件沙箱 | 2h | file_sandbox.py |
| 4. 审计日志 | 1h | audit_log.py |

**今天就可以完成！**

---

### 方案 B：完整实施

**总时间**: 10 小时

| 步骤 | 时间 | 产出 |
|------|------|------|
| 方案 A 全部内容 | 6h | 基础安全 |
| 5. 输入验证 | 2h | input_validation.py |
| 6. 安全中间件 | 2h | middleware.py |

**适合：对安全要求高的场景**

---

## ✅ 验收标准

### API Key 加密

- [ ] API Key 加密存储在 `.vault` 文件
- [ ] 密钥文件权限 600
- [ ] 解密功能正常
- [ ] 前端配置也加密

### 文件沙箱

- [ ] 禁止访问工作目录外文件
- [ ] 禁止创建危险文件
- [ ] 文件大小限制生效
- [ ] Agent 文件操作使用沙箱

### 审计日志

- [ ] API Key 使用记录
- [ ] 文件操作记录
- [ ] 安全事件记录
- [ ] 日志文件权限 600

---

## 📊 安全提升对比

| 指标 | 实施前 | 实施后 |
|------|--------|--------|
| API Key 存储 | ❌ 明文 | ✅ 加密 |
| 文件访问 | ⚠️ 基础限制 | ✅ 沙箱隔离 |
| 审计日志 | ⚠️ 分散 | ✅ 统一 |
| 前端安全 | ❌ 明文 | ✅ 加密 |

---

## 🚨 注意事项

### 密钥安全

```
✅ 要做：
- 密钥文件权限 600
- 不上传到 Git
- 定期备份.vault 文件

❌ 不要：
- 硬编码密钥
- 分享.vault 文件
- 使用弱主密码
```

### 性能影响

- 加密/解密：~1ms/次
- 审计日志：~5ms/次
- 文件沙箱：~2ms/次
- **总影响**: <10ms/请求（可忽略）

---

## 🎯 我的建议

**针对你的项目**：

1. **立即实施**：API Key 加密（P0）
   - 云端 AI 已上线
   - 用户 API Key 需要保护
   - 工作量小（2 小时）

2. **本周实施**：文件沙箱 + 审计日志（P1）
   - Agent 功能已有基础
   - 加强后更安全
   - 便于问题排查

3. **可选实施**：输入验证 + 中间件（P2）
   - 提升整体安全性
   - 时间充裕时进行

---

<div align="center">

## 🚀 快速总结

**现状**: 云端 AI 已集成，API Key 明文存储有风险

**计划**: 6 小时快速实施安全机制
- 2h: API Key 加密
- 1h: 前端加密
- 2h: 文件沙箱
- 1h: 审计日志

**产出**: 完整的安全保护机制

**现在就开始？告诉我你的选择！** 💪

</div>
