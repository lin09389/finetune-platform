[project]
name = "finetune-platform"
version = "2.1.0"
description = "AI-powered fine-tuning platform with CUA and MCP integration"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "ast-grep-cli>=0.42.1,<0.43",
    # ── Common control-plane runtime ────────────────────────────────
    "fastapi==0.109.0",
    "starlette==0.35.1",
    "uvicorn>=0.30.0,<0.31",
    "pydantic>=2.10.0,<2.11",
    "pydantic-settings>=2.1.0,<2.2",
    "python-multipart>=0.0.6,<0.1",
    "python-dotenv>=1.0.0,<1.1",
    "aiosqlite>=0.20,<0.21",
    "xxhash>=3.0,<4",
    # ── Runtime utilities ──────────────────────────────────────────
    "psutil==5.9.8",
    "requests==2.32.5",
    "chardet==5.2.0",
    "httpx==0.28.1",  # runtime HTTP client (ai/gateway.py, inference scheduler)
    "aiofiles>=23.0,<24",  # async file I/O for evaluation runs (api/evaluation.py)
    "anyio>=4.0,<5",  # async primitives (core/db_manager.py)
    # ── Logging ────────────────────────────────────────────────────
    "python-json-logger==2.0.7",
    # ── Security ───────────────────────────────────────────────────
    "python-magic-bin==0.4.14; sys_platform == 'win32'",
    "python-magic==0.4.27; sys_platform != 'win32'",
    "bcrypt>=4.0.0,<5",
    "cryptography>=42.0,<46",  # Fernet encryption (security/encryption.py)
    "PyJWT>=2.8,<3",
    # ── MCP and parsing ────────────────────────────────────────────
    "websockets>=14.0,<16",
    "pdfplumber>=0.10.0,<0.11",
    "pypdf>=4.0.0,<5",
    "python-docx>=1.1.0,<1.2",
    "openpyxl>=3.1.0,<3.2",
    # ── Scheduler and clipboard ────────────────────────────────────
    "croniter>=1.3.0,<2",
    "pyperclip>=1.8.2,<1.9",
]

[project.optional-dependencies]
agent = [
    # DeepAgents / LangGraph agent framework. Does not include local RAG
    # embeddings or CUA input drivers; compose the API image with rag+cua when
    # those features are enabled.
    "deepagents>=0.6.3,<0.7",
    "langchain>=1.3.0,<2.0.0",
    "langchain-core>=1.4.0,<2.0.0",
    "langchain-deepseek>=1.0.0,<2.0.0",
    "langgraph>=1.2.0,<1.3.0",
    "langgraph-checkpoint-sqlite>=3.0.3,<3.1",
]
rag = [
    # Local Knowledge/RAG. sentence-transformers depends on PyTorch, so an API
    # image with local embeddings is not Torch-free until embeddings move behind
    # a remote provider.
    "chromadb>=0.4.22,<0.5",
    "sentence-transformers==5.2.3",
    "torch==2.2.2",
    "pandas>=2.1,<3",
    "pyarrow>=15.0.0,<20",
]
cua = [
    # Experimental computer-use and OCR runtime.
    "mss==9.0.1",
    "Pillow==10.2.0",
    "pytesseract>=0.3.10,<0.4",
    "pynput==1.7.6",
    "pyautogui>=0.9.54,<0.10",
    "pywin32>=305,<306; sys_platform == 'win32'",
    "pywinpty>=2.0.13,<3; sys_platform == 'win32'",
    "rapidocr-onnxruntime==1.4.4",
]
modelhub = [
    # Control-plane model catalog/search/download integrations.
    "huggingface-hub==0.36.0",
    "modelscope>=1.10.0,<2",
]
model-ops = [
    # Existing /models conversion, merge, quantization, and export routes still
    # execute in the API process. Keep this separate from training datasets and
    # worker-only dependencies so it can be moved behind a worker later.
    "torch==2.2.2",
    "transformers==4.46.3",
    "peft==0.18.1",
    "safetensors==0.7.0",
    "onnx==1.15.0",
    "onnxruntime==1.16.3",
]
training = [
    # GPU training worker and model management operations.
    "torch==2.2.2",
    "transformers==4.46.3",
    "accelerate==1.13.0",
    "peft==0.18.1",
    "scipy==1.11.4",
    "scikit-learn==1.5.2",
    "joblib==1.4.2",
    "datasets==3.6.0",
    "huggingface-hub==0.36.0",
    "safetensors==0.7.0",
    "pandas>=2.1,<3",
    "pyarrow>=15.0.0,<20",
    "modelscope>=1.10.0,<2",
    "onnx==1.15.0",
    "onnxruntime==1.16.3",
]
inference = [
    # Local model execution service.
    "torch==2.2.2",
    "transformers==4.46.3",
    "accelerate==1.13.0",
    "peft==0.18.1",
    "llama-cpp-python>=0.2.79,<0.3",
    "aiohttp>=3.9,<4",
    "grpcio==1.71.0",
    "grpcio-tools==1.71.0",
]
all = [
    "deepagents>=0.6.3,<0.7",
    "langchain>=1.3.0,<2.0.0",
    "langchain-core>=1.4.0,<2.0.0",
    "langchain-deepseek>=1.0.0,<2.0.0",
    "langgraph>=1.2.0,<1.3.0",
    "langgraph-checkpoint-sqlite>=3.0.3,<3.1",
    "chromadb>=0.4.22,<0.5",
    "sentence-transformers==5.2.3",
    "mss==9.0.1",
    "Pillow==10.2.0",
    "pytesseract>=0.3.10,<0.4",
    "pynput==1.7.6",
    "pyautogui>=0.9.54,<0.10",
    "pywin32>=305,<306; sys_platform == 'win32'",
    "pywinpty>=2.0.13,<3; sys_platform == 'win32'",
    "rapidocr-onnxruntime==1.4.4",
    "huggingface-hub==0.36.0",
    "modelscope>=1.10.0,<2",
    "torch==2.2.2",
    "transformers==4.46.3",
    "accelerate==1.13.0",
    "peft==0.18.1",
    "llama-cpp-python>=0.2.79,<0.3",
    "aiohttp>=3.9,<4",
    "grpcio==1.71.0",
    "grpcio-tools==1.71.0",
    "scipy==1.11.4",
    "scikit-learn==1.5.2",
    "joblib==1.4.2",
    "datasets==3.6.0",
    "huggingface-hub==0.36.0",
    "safetensors==0.7.0",
    "pandas>=2.1,<3",
    "pyarrow>=15.0.0,<20",
    "onnx==1.15.0",
    "onnxruntime==1.16.3",
    "bitsandbytes==0.41.3",
]
dev = [
    "pytest==7.4.3",
    "pytest-cov==4.1.0",
    "pytest-asyncio==0.23.2",
    "ruff>=0.1.0,<0.2",
    "mypy>=1.0.0,<1.9",
    "black>=23.0.0,<24",
    "playwright>=1.40.0,<1.41",
    "safety>=2.0.0,<3",
]
gpu = [
    "bitsandbytes==0.41.3",
]

[tool.uv]
package = false  # not a publishable package; skip build-metadata strict checks

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "*.egg-info",
    "backup_old_modules",
    "data",
]

[tool.ruff.lint]
select = [
    "E",
    "W",
    "F",
    "I",
    "B",
    "C4",
    "UP",
    "ARG",
    "SIM",
]
ignore = [
    "E501",
    "B008",
    "B904",
    "ARG001",
    "ARG002",
    "SIM108",
]

[tool.ruff.lint.isort]
known-first-party = ["agent", "api", "core", "cua", "mcp", "skills", "memory", "rag", "security", "context", "workspace", "ai", "backends"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = [
    "backup_old_modules",
    "data",
    "tests/",
]

[[tool.mypy.overrides]]
module = [
    "pyautogui.*",
    "mss.*",
    "pytesseract.*",
    "PIL.*",
    "chromadb.*",
    "transformers.*",
    "torch.*",
    "playwright.*",
    "redis.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["server/tests", "tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short --strict-markers"
markers = [
    "unit: marks tests as unit tests",
    "slow: marks tests as slow",
    "integration: marks tests as integration tests",
    "e2e: marks tests as end-to-end tests",
]
asyncio_mode = "auto"

[tool.coverage.run]
source = ["server"]
branch = true
omit = [
    "*/tests/*",
    "*/backup_old_modules/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
fail_under = 60
show_missing = true
skip_covered = true

[tool.uv.sources]
torch = { index = "pytorch-cu121" }
torchaudio = { index = "pytorch-cu121" }
torchvision = { index = "pytorch-cu121" }

[[tool.uv.index]]
name = "pytorch-cu121"
url = "https://download.pytorch.org/whl/cu121"
explicit = true
